# 项目上下文与维护记录

> 用途：为后续开发、排查和代码审查保存项目级背景。本文是持续更新的“活文档”，不是用户操作手册。
>
> 最近更新：2026-09-17；用户管理支持部门层级搜索分组、最高层岗位直属上级豁免及 admin 独立微信通知地址。ECN 拆分及类型检查约束见下文和根目录 AGENTS.md。

## 1. 项目概览

- 项目名称（由程序标题推断）：研发项目文件管理系统。
- 技术形态：基于 NiceGUI 的 Python Web 应用，页面、交互和后端业务逻辑主要都在 Python 中实现。
- 入口：`src/main.py`。根路由跳转登录页，应用默认监听 `0.0.0.0:8080`。
- 页面注册：`src/main.py` 导入 `src/pages`，`src/pages/__init__.py` 再导入所有页面模块，从而执行各页面的 `@ui.page` 路由注册。
- 主要业务范围：项目资料/概述、需求、ECN 工程变更、异常管理、样品问题与样品单、统计、设计知识及工程工具。
- 登录态：页面通常从 `app.storage.user` 读取 `current_user`、`current_role`；ECN 未登录时跳转 `/login`。
- 登录页会复用同一浏览器现有会话，并复核账号仍在职；ECN未登录跳转携带返回地址，登录成功后回到ECN。会话以浏览器Cookie识别，localhost、IP、域名或不同浏览器配置不自动共享；部署服务器本身不能解决地址或浏览器不一致的问题。通知地址应与员工日常登录入口统一。
- 本地命令行和 VS Code 的 NiceGUI 调试配置均默认启用热重载；watchfiles 强制使用 200ms 轮询，以可靠检测 VS Code 或 Codex 写入的 Python 文件。调试配置通过 `subProcess=true` 跟随 NiceGUI 重载后的子进程；开发模式浏览器重连等待为30秒，用于覆盖debugpy重启子进程约15秒的间隔，避免源码保存后被误判为永久断联。需要临时关闭热重载时可显式设置 `NICEGUI_RELOAD=false`。

## 2. 数据与运行时架构

### 2.1 持久化

- 统一存储模块：`src/db_storage.py`。
- SQLite 文件：`db/nicegui_storage.db`。
- `general_storage`：顶层 key 对应一段 JSON，兼容 `get_item`、`set_item`、深层路径读写和原子深层更新。
- `json_entity_storage`：按 `namespace + entity_id` 拆分的实体存储，供已迁移的业务模块使用。
- 读取优先走进程内缓存，默认返回深拷贝；写入使用异步 SQLite、进程内资源锁及数据库事务锁。
- 应用启动时 `master_startup()` 先执行 `db_storage.init_db()`，然后加载概述配置和各后台任务；关闭时关闭数据库。

### 2.2 NiceGUI 全局数据

项目资料相关的运行时索引主要放在 `app.storage.general`，ECN 直接依赖以下键：

- `project_summary`：项目名称及项目状态，用于生成可选项目树。
- `project_req_max_ver`：每个项目当前最高需求版本，执行系统内资料方案时用于生成新概述节点的版本激活信息。
- `over_config_data` / `over_config_data_flat`：概述技术维度、分组、字段配置和处理类型。
- `requirement_overview_impact_config`：从根目录 `requirement_overview_impact.json` 校验加载的
  `node_id -> overview label` 影响映射；审批过程只读取该内存配置。
- `user_preferences`：头像等用户偏好。

管理员页“配置需求对概述影响”工具以 `app.state.init_config_data` 提供需求节点及说明，
以 `app.storage.general["over_config_data_flat"]` 提供合法概述项，并优先从当前审批内存载入草稿。
保存时先校验全部映射，再原子替换 `requirement_overview_impact.json` 并同步
`requirement_overview_impact_config`；内存写入失败会恢复旧文件和旧内存，因此保存成功后无需重启服务。
落盘及同步到内存的 `node_impacts` 均按 `node_id` 数值升序排列，避免按字符串顺序把 `10` 排在 `2` 前面。
显式配置空数组表示该节点不影响任何概述，删除节点映射则按 `unmapped_policy` 执行全概述兜底或阻止审批。

需求审批不会先覆盖正式概述文件：系统按目标版本生成唯一候选文件，从该版本的
`added` / `deleted` / `modified` 提取 `node_id`，原子更新概述状态后才发布候选文件并标记已审。
发布失败时会恢复文件快照和审批状态，并以原子更新内采集的概述前后快照进行比较回滚。
需求版本差异按有效业务答案比较：单选/下拉单选只比较当前选中值，多选只比较值为 `True`
的已选项集合。模板增删未选选项而产生的缺失键或 `False` 键不属于需求变更，不应生成
“更改为”记录或触发关联概述待定；实际选中值、已选集合、公差或引用文件变化仍属于变更。

静态文件目录主要为 `img/`、`uploads/`，文件 URL 前缀为 `/files`。

### 2.3 概述操作记录

- 概述新建和手动激活状态调整的原因选项由根目录 `overview_operation_config.json` 管理；“其他”需要补充简短说明。自动转待定、关联影响、表格联动及 ECN 落盘的原因文案也集中在该配置中。
- 顶层 `creator` 表示概述的原始录入人，后续状态调整不覆盖它；“最近操作人”和技术维度的“最近负责人”读取最新一条 `timestamp.<时间>.creator`。
- 新概述不再生成顶层 `notes`。每次操作在秒级 `timestamp` 中保存 `creator`、`reason` 和当时完整的 `select_activ_dic` 状态快照；ECN 自动处理额外以 `source_id` 保存 ECN 编号。既有概述的顶层 `notes` 只作为历史兼容读取，不再更新。
- SVN 类概述保留按 `search_scope_regular` 定位项目目录的首选规则，`search_hierarchy` 仅参与该正则路径；首选 URL 未找到文件时，可按概述项的 `fallback_folder_path` 从同一 `upload_path/state_path` 起点进入相对兜底目录并直接探测文件。空值表示不启用兜底。
- 自动转待定不写“系统”为操作人，使用该概述技术维度已有的最近负责人；没有运行时负责人索引时回退该条概述的原始录入人。
- 概述 Tooltip 的“注释”只显示最新一条时间记录的 `reason`；旧数据没有 `reason` 时回退旧 `notes`。单条、具体参数和项目技术维度三级历史都可分别查看录入记录及后续状态变更，并展示原因、操作人、状态快照和可选来源编号。
- 检测概述汇总表继续使用“备注”作为列名，但内容读取最新一条 `timestamp.reason`，旧数据仍回退顶层 `notes`。

## 3. ECN 管理模块

### 3.1 模块定位

- 入口：`src/pages/ecn_management.py`；实现位于 `src/modules/ecn/`，具体职责见该目录 `README.md`。
- 路由：`/ecn_management`。
- 业务含义：先提交 ECR（工程变更请求），经过可行性审批后协同编制 ECN 影响与实施方案，再经过方案评审、研发助理事项确认、系统内资料自动执行和物料责任项确认，最终关闭单据。
- 页面结构：主页面是可搜索、按状态筛选和分页的 AG Grid ECN 总表，交互及整体风格与样品问题模块一致；详情使用最大化对话框，包含“ECR-申请、ECN-影响、ECN-方案、ECN-执行、审批记录”五个页签。
- “ECN-方案”和“ECN-执行”包含大量动态控件，首次进入相应页签时才创建；协同轮询可先更新本地数据，页签打开后仍按最新状态渲染。
- 审批记录和企业微信通知只显示方案顺序编号、事项名称、项目及责任范围等业务信息，不显示方案 UUID、`__erp__` 或物料责任项内部键；已有开发记录在界面显示时同步转换。

### 3.2 ECN 数据模型

`get_ecn_template()` 是新建ECN时使用的唯一标准结构；现阶段不保留旧单据字段迁移或模板补齐逻辑。主要字段如下：

- `ecn_id`、`form_no`。
- `basic_info`：标题、申请部门/人/日期、需求日期、文件号、永久/临时性质、ERP 单号、变更原因、变更要求及原因说明。
- `target_projects`：ECR 初始变更项目。
- `review_info`：扩大影响项目、影响维度、必出方案的资料项、物料类别与动作矩阵。
- `execution_info`：分阶段执行状态；包含普通资料事项确认、ERP确认、系统内资料逐方案执行结果及物料方案逐责任项确认记录。每个勾选项保留确认人、角色、时间和变更历史；`attachments` 按执行节点保存附件清单。
- `change_items`：具体变更方案。
- `basic_info.attachments` 保存 ECR 申请附件；其它特定事项/资料方案在对应 `change_items` 行保存附件清单。文件根目录由 ECN JSON 配置，按上传人、ECN 编号和业务位置分层归档，附件读取按最新单据权限校验。ECR 申请人编辑草稿或驳回单时，附件清单和上传控件直接嵌在表单内；审核及后续阶段只直接展示已有附件，无附件时隐藏该区。方案表格的附件图标只查看；方案编辑弹窗左右展示旧附件清单和上传控件，文件行留有间距。执行阶段的特定事项、ERP 和物料节点在勾选框右侧最多显示一个附件按钮：青绿色上传图标供负责人查看并上传，蓝紫色回形针仅在有附件时供其他人查看；上传或删除成功立即更新按钮悬停数量。系统内资料执行结果不显示附件按钮。
- `workflow`：当前状态、阶段、步骤、路由类型、待处理角色、当前步骤审批结果和方案参与人状态。
- `approval_log`：审批及操作时间线。
- `timestamp`：单据内的业务时间记录；跨客户端列表刷新另用全局版本戳。

数据结构策略：ECN 仍处于开发阶段，服务器尚无需要保留或收尾的既往 ECN 正式记录。ECN 记录只读取当前结构；调整字段时直接同步修改模板、写入、读取、展示和测试，不增加旧字段回退、历史审批收尾或单据数据迁移分支。本地开发测试数据不视为服务器历史兼容约束。该前提只适用于 ECN 业务记录，不代表可以跳过服务器用户身份数据的迁移和权限配置。

### 3.3 编号与项目范围

- 编号格式：`ECN + YYMMDD + 日序号（至少两位）`，例如 `ECN26080701`。新建窗口显示“保存后生成”；首次保存时在读取最新集合的 SQLite 事务中分配编号并插入，避免并发撞号。
- 新建时扫描当天已有单号，取最大两位尾号加一。
- 数据库身份模式下，申请部门读取申请人的主部门名称；旧 Excel 模式没有组织数据时才按当前角色关键词推导，未匹配时为“其它部门”。
- ECR 初始变更对象只能从状态为“试产”或“量产”的项目中选择。
- 进入方案阶段后，可把影响范围扩大到其他试产/量产项目，也可扩大到非试产/量产项目。
- 客户/在途追溯节点优先指定各项目销售；项目销售无法识别时，任务按“销售主管”责任路线授权和通知。企业微信绑定只负责把已识别的系统处理人映射为收件账号，不替代ECN执行权限。
- 物料执行负责人按身份库的主任职和直属上级逐级解析：当前具体负责人或岗位层级没有在职且具备ECN执行权限的人员时，只传给第一层可处理的直属上级，不同时扩大到更高层。销售链明确区分项目销售、销售主管和销售总监；主管可处理时总监不接该上提待办，主管不可处理时才继续到总监，项目销售的直属上级若本来就是总监则直接交总监。完全未识别项目销售时从销售主管层开始查找。企业微信绑定仅决定已解析人员能否收到微信，不参与责任层级判断。

### 3.4 状态机与审批路由

主干流程：

```text
草稿 / 已被驳回
  -> ECR 审批中
  -> ECN 方案编写与确认中
  -> ECN 方案评审中
  -> ECN 料号补充中（仅物料方案缺少料号时）
  -> ECN 执行确认中
     1. 研发助理确认事项/资料与ERP
     2. 系统内资料逐条自动落盘
     3. 物料追溯与旧料处置责任项确认
  -> 变更已完成
```

旁路状态：ECR 审批中可由申请人撤回至草稿或作废；任一 ECR 审批驳回进入“已被驳回”；方案评审驳回回到方案编写阶段，并按所选方案定向进入整改和重新确认。

ECR 与 ECN 方案评审分别使用 `ecn:ecr_review`、`ecn:scheme_review` 业务事件；管理员在 `/manage` 按申请人主部门/主岗位配置流程条件、串行节点、节点内任意审批或全部会签，提交时固化流程版本和具体审批用户名。ECN 不再从根目录 JSON 读取审批路线：

ECN等级使用稳定编码 `simple / general / complex`，显示名称由 `ecn_management_config.json.ecn_levels` 配置；未判定时按 `general` 运行。`ecn.level.classify.ecr` 允许在ECR审批阶段判定，`ecn.level.classify.scheme` 允许在方案评审发起前判定。ECR审批中切换进入或离开简单等级时，在ECN单据与待办的同一SQLite事务内终止原当前待办并重建审批快照，避免显示等级与实际路线不一致。简单等级使用独立事件 `ecn:ecr_review_simple`、`ecn:scheme_review_simple`，供管理员配置精简路线；一般和复杂等级沿用现有审批事件。

复杂等级可由 `ecn.validation.designate` 指定部分方案提交验证报告。报告由方案出具人通过现有自定义上传控件提交，`ecn.validation.report.view` 控制其他人员查看下载，`ecn.validation.report.approve` 与查看权限共同控制审批，且作者不能审批本人报告。指定报告全部上传并审批通过前，后端拒绝发起方案评审；上传、删除或方案内容修改会使原审批失效。首页“待我处理”和企业微信依次提醒方案作者上传、验证审批人审核。

- 销售发起 ECR：销售总监 -> 研发经理。
- 非销售发起 ECR：研发经理 -> 销售总监。
- ECN 方案评审：研发经理 -> 销售总监 -> 工程、质量、PMC 并行会签。
- ECN 方案评审阶段的审批人底部操作栏不显示通用“审批意见”输入框：“同意”直接按空意见提交；“驳回”必须进入具体方案驳回弹窗，在其中选择被驳回方案并填写驳回意见。ECR 审批和其它流程阶段仍保留原有通用意见输入方式。
- “被驳回方案”多选清单固定以选框左下角为锚点、从选框下方展开，不再根据空间自动翻到上方；清单限制最大高度并在内部滚动。
- “审批记录”页签不使用 NiceGUI 默认时间轴，改为紧凑纵向日志列表：每条记录在同一行组织动作、审批人/角色和时间，意见仅在存在时占用下一小行，并以轻量斑马纹区分相邻记录。当前节点待审批/已同意摘要固定在顶部，历史记录区填满页签剩余高度并独立纵向滚动，节点较多时不会越出外层卡片。
- 方案评审全部通过且物料料号齐全后生成 `execution_info` 清单并进入分阶段执行。系统从七个已发布的 `ecn:execution_*` 流程读取追溯范围内的责任节点、并行阶段、稳定岗位和权限，把流程版本及具体责任人随执行项固化；缺少数据库身份或已发布流程时明确拒绝进入执行。旧料处置不再配置或生成独立执行责任项。
- 物料方案按新增/仅删除/调量保存主料号，按更改分别保存改前与改后主料号；新增方案还可逐行添加新增替换料，更改方案可分别逐行添加更改前、后替换料，每条替换料保存稳定ID、料号和物料名称。方案编写时全部料号允许暂缺，替换料名称必须填写；当前内容、执行后结果及执行表同步展示对应替换料。方案评审全部通过后，主物料或替换料存在缺失料号的单据进入“ECN 料号补充中”，拥有 `ecn.material_code.edit` 的人员收到首页及企业微信待办，并可在方案表操作列补充或修改全部料号。最后一项料号补齐时才在同一单据事务中生成 `execution_info` 并进入执行；进入执行后料号锁定。

流程快照分别保存在 `workflow.ecr_workflow_assignment` 和 `workflow.scheme_workflow_assignment`，`work_assignments` 保存当前节点具体人员待办；`pending_roles` 只保留当前具体用户名供原界面展示，不参与授权。审批日志记录用户、岗位显示文本、动作、时间及可选意见。

### 3.5 权限逻辑

- ECN 权限集中在 `src/permission_catalog.py`，判断集中在 `src/ecn_access.py`。入口/详情使用 `ecn.view`；本人 ECR 新建、草稿、提交、撤回和作废使用 `ecn.request.create`；影响维护、方案编写、发起方案评审分别使用 `ecn.impact.edit`、`ecn.scheme.edit`、`ecn.scheme.review.submit`。
- ECN等级在ECR审核和方案评审前分别使用 `ecn.level.classify.ecr`、`ecn.level.classify.scheme`；复杂方案验证分别使用 `ecn.validation.designate`、`ecn.validation.report.view`、`ecn.validation.report.approve`，查看与审批不互相隐含。
- ECR 与方案审批资格分别为 `ecn.ecr.approve`、`ecn.scheme.approve`，资格权限本身不能审批任意单据；用户还必须持有本单当前节点的具体 `work_assignments` 待办。`admin` 只有在流程明确选中时才成为审批人。
- 审批协调人使用独立权限 `ecn.approval.reassign`。授权后可在单据“审批记录”页签把当前或后续未完成节点中的某一审核人移交给另一名在职人员；接手人必须同时拥有ECN查看权限和该节点要求的ECR/方案审批权限。已完成节点和已投票人员不可改写。当前节点移交会在同一SQLite事务内同步更新流程快照、具体待办和审批日志，原审核人立即失去该单待办；后续节点只调整快照，到达该节点时再生成新审核人的待办。
- 第一阶段资料/ERP确认及系统内资料落盘使用 `ecn.execution.assistant`。物料责任节点按 `ecn.execution.purchase.confirm`、`ecn.execution.pmc.confirm`、`ecn.execution.production.confirm`、`ecn.execution.sales_supervisor.confirm` 判断；项目销售还必须是单据明确固化的用户名并拥有 `ecn.execution.material.confirm`。数据库模式不再读取当前岗位名称匹配这些责任项。
- 执行待办动态复核人员状态和稳定权限：指定人停用、离职或失去权限后，原待办不会被视为可处理；首页和执行页标记负责人异常，企业微信提醒执行助理改派。物料岗位池只在没有任何在职合资格人员时判定异常；执行助理可把异常物料节点改派给具备ECN执行权限的在职人员，改派和后续确认均在最新单据事务中校验并留痕。
- ECN列表、执行详情渲染和后台提醒扫描使用单轮用户权限快照：身份库通过一次批量查询返回全部在职用户有效权限，同轮所有单据和物料节点复用，避免在事件循环中按单据、节点、用户反复同步打开SQLite连接。快照只用于展示与待办候选计算；确认和改派仍在写事务中读取实时账号状态与权限。
- 普通资料非图片附件按图纸、SOP、测试报告、其它四类独立 `ecn.file.ordinary.*.view` 权限；系统内资料附件复用对应项目概述 `label` 的查看/维护权限。图片缩略图继续按原展示规则。永久删除使用 `ecn.delete`。
- 页面入口、按钮和关键保存/删除/审批/执行回调都会复核权限；状态责任、申请人本人、方案作者本人和具体审批待办仍是权限之外的业务范围约束。

### 3.6 影响与方案完整性

- “相关影响”是告知型维度。
- 勾选的资料类型和“物料类别-动作”组合都是强制交付项。
- 资料方案分成两个并列大类，并各自使用独立新增入口及折叠栏：
  - 普通资料变更方案：`text_desc` / `scheme_category=ordinary_document`，不直接绑定概述数据。
  - 概述资料变更方案：`overview_update` / `scheme_category=overview_document`，与项目概述字段绑定，可选择更新已有节点或添加节点，支持多项目统一方案及文件类字段。
- ECN 方案折叠栏固定按“系统内资料变更方案、普通资料变更方案、物料变更方案”排列；没有条目的分类不显示。异常分类只进入“未识别方案”防护组，不进行字段推断。
- 普通资料、物料变更和系统内资料方案使用同一套可选项目范围：ECR申请时填写的 `target_projects`，加影响评审返回的 `expanded_projects_mass` 与 `expanded_projects_non_mass`，按该顺序去重展示；三类方案都必须选择至少一个目标项目，并统一保存到方案条目的 `projects` 字段。
- 方案展示采用“资料分类折叠栏 + 舒适型对比表格”，不再按项目增加第二层折叠。每个方案固定一行，列为编号、变更对象/类别、项目、动作、当前内容、执行后结果、追溯、处置、变更要求、影响勾选、处理人、状态和操作；变更要求独立显示 `req_idxs`，影响勾选列分开列出关联资料和物料动作，无关联时明确显示“未关联”。系统内资料的多项目统一方案在项目、动作、当前内容、执行后结果四列使用等高子行横向一一对应，避免靠上下顺序猜测归属。每个分类表格内按实际展示顺序使用白色与极淡蓝色斑马纹，相邻方案行底色不同，悬停时统一加深；原内容保留原样、不使用删除线，处理人直接显示完整姓名。有驳回历史的方案在操作列显示历史按钮，点击后通过独立弹窗按最新在前查看全部驳回意见、审核人和时间，不再在数据行下方展开。表格常态使用中性色，仅待改进/待重新确认/完成使用语义色，窄视口通过表格区域横向滚动保持各列对齐。三个正常方案分类及异常防护组的展开状态在详情弹窗生命周期内独立保存；新增、修改、删除、确认状态或后台同步触发列表重绘时沿用用户当前选择，不再自动全部展开。
- 系统内资料方案的“执行后结果”不重复显示动作列已经表达的“新增、更新为”等前缀，只展示实际结果内容；新增时是否保留现有内容、更换时原内容是否失效等补充语义放入结果内容的 tooltip。失效动作的执行后结果使用简短灰色提示“仍有有效内容”或“该参数将为空”；悬停后展示当前项目、具体参数及剩余有效内容，或说明已无有效概述。按当前需求版本从该项目同一参数中排除本次失效的节点计算（包含其它基准行），只预览本条失效操作，不合并本单其它尚未执行的方案。
- ECN方案表格的文件 tooltip 不显示内部、本地或SVN文件URL；可访问文件只提示对应动作结果说明，无结果说明时不创建空tooltip。文件不存在和角色无权限等会影响用户操作的状态信息仍可在tooltip中显示。
- ECN表格中可查看或下载的非图片文件不使用蓝色链接文字：当前内容列沿用原内容的深灰色，执行后结果列沿用新内容的近黑色，仅通过手型指针和悬停下划线表达可点击，减少页面颜色干扰。
- ECN表格内图片缩略图旁的文件名与普通文件名统一使用 `text-sm`、半粗体和所在列的语义文字色，不再使用更小、更浅的独立样式；缩略图本身尺寸保持不变。
- ECN当前内容列展示原有 `search` 概述文件时，不能用 `upload_path/文件名` 简单拼接判断。若节点没有有效的 `local_file_path`，展示层会按该概述参数配置调用与概述模块相同的 `validate_search_path`，结合当前项目依次应用 `upload_path`、`search_scope_regular`、`search_folder_according` 和 `search_hierarchy` 异步查找；唯一命中且本地文件存在才注册静态地址并显示可查看文件，完整检索失败后才显示“文件不存在”，具体失败原因放入tooltip。
- ECN 方案页签内容最大宽度为 `1900px`，在常见 1920px 宽屏下尽量利用弹窗可用空间；视口不足时仍由方案表格区域横向滚动，不影响其它页签的宽度布局。
- 非基准列选择“作为新数据添加”并绑定基准行后，系统按 `row_id + 当前需求版本` 检查该具体参数的同一格是否已有激活数据；若已有，在选择框右侧显示不占额外行高的红色警告按钮。用户首次选中该冲突组合时，浮层自动在按钮下方弹出并按 JSON 配置的秒数关闭；后续可点击按钮再次查看当前内容及本单已暂存的同格新增内容。该提示只警示“一格多数据”，不禁止确有需要的继续新增。
- 系统内资料方案把新增位置当时的数据库内容和本单其它暂存新增固化到 `project_states.<项目>.existing_contents`。表格使用“新增 / 更换 / 失效”动作，并为每个项目明确展示“当前内容”和“执行后结果”：新增且数据库当前已有内容时只用淡色文字显示“存在现有内容”，悬停后再查看具体内容，避免展示大概率与本次新增无关的原文；更换说明原内容失效并显示更换目标，失效固定说明不生成新内容。第一列新增统计该参数当前全部激活内容，非第一列新增只统计所绑定基准行的同格内容。
- 系统内资料方案表格只有上传图片使用 `components.FileThumbnail` 缩略图；`search`、`svn` 以及上传的 PDF、Office、压缩包、视频等非图片文件均直接显示带附件图标的可点击文件名，避免普通文件占用过多表格空间。PDF点击后在新标签页查看，其它格式点击下载。`search` 校验成功时将唯一命中的本地路径随方案保存，以便ECN展示时注册静态文件并提供真实查看/下载；`svn` 使用逐项目校验后保存的远程URL，并沿用概述区语义由服务端使用配置账号读取，远程PDF进入现有预览缓存后打开，其它远程文件下载。文件缺失时只显示失效提示。该能力只接入ECN展示层，不修改 `components.py` 内既有逻辑。
- ECN表格中的非图片文件查看/跳转在数据库模式使用稳定权限：系统内资料方案复用对应 `project_overview.item.<label>.view/edit`，其它特定事项/资料按变更分类使用四项 `ecn.file.ordinary.*.view`。旧 Excel 模式才读取概述 `read_role/edit_role` 与 ECN JSON 的分类角色关键字。无权限时显示锁定文件名且不注册点击动作；上传图片缩略图仍不套用该项非图片权限。
- `search`、`svn` 等路径校验型概述方案不能只依赖布尔状态：校验成功时会保存由“处理类型、文件名、目标项目顺序、技术维度、具体参数”组成的校验签名。输入或上下文变化会立即失效，确认保存时再次严格比对；校验异步执行期间发生变化也不会接受旧校验结果。编辑弹窗打开时根据已有具体参数主动从 `over_config_data_flat` 初始化完整配置和处理类型，不依赖用户切换选择框触发；同时使用方案数据的深拷贝草稿，校验时直接读取当前输入控件值，收集本单暂存依赖覆盖时排除正在编辑的方案，避免一次失败输入污染原方案或反向覆盖自己的校验依赖。
- SVN 类型的系统内资料方案必须对每个需要新增内容的目标项目分别校验，不能复用首个项目的结果。`over_config_data` 加载后生成的 `over_config_data_flat` 保留具体参数的 `state_path` 配置；ECN 根据 `project_summary.<项目>.state` 为研发、转产、试产、量产等状态分别选择仓库目录，并逐项目调用 SVN 校验。全部项目均通过后，URL、文件类型和仓库目录保存到 `project_states.<项目>.new_file_data`，最终执行时再与统一的新内容合并写入各项目概述节点；任一项目失败都会阻止方案保存。只做原内容失效、不生成新内容的项目不参与 SVN 新文件校验，校验成功或失败提示均明确列出这部分免检项目及数量，避免用户误以为项目被遗漏。
- 系统内资料方案的文本、检测类型与项目概述录入保持一致：存在新增或更换项目且新内容为空时，自动带入该参数配置的 `dialog_placeholder` 作为格式示例；已有方案内容不会被覆盖。文件、图片、视频及路径校验类型仍使用各自的上传或文件名提示；全部项目均选择失效时不要求新内容或路径有效性校验。
- 物料方案：`text_desc` / `scheme_category=material`，方案分类由 `scheme_options.material_change_types` 的稳定语义键配置（`add / adjust_quantity / discontinue / replace`），当前文案为“新增、调量、弃用、更换”，明细只以结构化 `material_change` 保存。新增/弃用记录物料名称、用量、单位；调量记录物料名称、改前/改后用量、单位；更换分别记录改前和改后的物料名称、用量、单位。默认单位由 `scheme_options.material_default_unit` 配置，当前为 `pcs`。方案表格沿用“变更前 → 变更后”对照：新增显示“无 → 新物料”，弃用显示“旧物料 → 弃用”，调量及更换并列显示前后值。
- 每条物料方案必须选择至少一个追溯处置范围，只保存为 `traceability_levels: list`。`disposition_measure: str` 严格表示旧料处置措施，仅 JSON 配置 `scheme_options.material_disposition_required_types` 指定的稳定语义键对应分类显示并必填，当前为 `discontinue / replace`（弃用、更换）；`add / adjust_quantity`（新增、调量）及所有非物料方案不显示、不保存该字段。非物料方案的追溯处置范围为选填。可选值集中在 `ecn_management_config.json` 的 `scheme_tracking` 和 `scheme_options` 中维护。必填字段缺失时完整性看板会提示并阻止发起评审，相关内容也进入驳回前后方案快照。
- 追溯等级按 JSON 数组顺序表示由浅到深，并在系统内资料、普通资料和物料方案弹窗中直接平铺为复选框；仅在用户新勾选某一级的当次操作中，自动扩选该项上方全部等级。后续手动取消任意中间等级时保留断层，不自动补回；打开、保存和展示阶段也不做连续化规范。适用物料分类的旧料处置措施直接平铺为单选框，不再使用下拉框。工程师选择“有条件用完止”时弹出旧料价值判断，不再填写具体使用条件：高价值自动换成“暂存移用”，低价值自动换成“报废”；“有条件用完止”只是选择触发项，不能作为最终方案保存或进入评审。方案表格将“追溯等级”和“处置措施”作为独立列：选填方案已配置时显示具体值，未配置时显示“—”；物料必填项缺失时显示“未配置”。
- 方案表格“旧料处置措施”主文字与“执行后结果”使用相同字号；报废、返工依次使用红、橙警示色，其它处置措施保持中性灰色。
- 每个方案都可记录关联的目标项目、ECR 要求序号，并按类别关联已勾选的资料或物料项。添加/编辑弹窗通过直接展开的复选框组选择这些关联项，避免下拉多选中内容截断和反复开合；系统内资料方案勾选项目后实时重建多项目配置矩阵。底层仍保存为原有列表字段。界面将关联区标为必填，但当前单条方案保存回调并未强制校验要求/影响关联列表非空。
- 其它特定事项/资料变更方案的分类仍是单值 `change_type`，但添加/编辑时将 JSON 配置的全部分类平铺为单选项，不再使用下拉框。普通资料方案可选“提供文件服务器存放路径说明”；勾选后路径必填并保存为 `file_server_path`，方案表格不直接铺开路径，仅在执行后结果旁显示低调的文件夹图标，悬停 tooltip 查看完整路径。驳回方案的业务快照会保留该字段。
- 覆盖率看板同时检查三类强制关联：每一条 ECR 变更要求、需强制覆盖的已勾选资料、已勾选物料动作。`ecn_management_config.json` 的 `scheme_review.document_types_without_required_scheme` 可列出不强制要求对应方案的资料类型；这些资料仍可主动关联方案。任一变更要求没有至少一个方案通过 `req_idxs` 关联，或其余资料/物料有缺项，系统都会显示具体遗漏项并阻止发起 ECN 方案评审；物料追溯/处置缺失提示使用当前方案表格的全局编号（如“方案 #03”），不显示内部 UUID。“方案已齐”状态和入口提醒复用同一覆盖口径。
- `ecn_management_config.json` 只保存方案选项、追溯范围、状态文案和通知参数，不再保存 ECR、方案评审或执行责任路线。七个追溯执行事件均由管理员审批流程维护，方案评审完成时固化流程版本、节点阶段和具体人员；“项目销售”节点仍按项目资料中的具体用户名解析。执行人停用后的逐级传导继续读取身份数据库中的直属上级关系。
- ECN 影响区对扩大影响项目的添加/删除，以及“相关影响”选项的勾选/取消进行字段级审计，追加保存在 `review_info.impact_change_log`。每条记录包含唯一 `event_id`、`user`、`role`、`time`、`field`、`target`、`action`、`before`、`after`；原子保存按 `event_id` 追加去重，不用某个用户的本地副本覆盖已有日志。`workflow.impact_handlers` 仍只表示参与维护影响区的人员名单，不能代替字段级审计。
- 添加或修改方案会把当前用户加入 `scheme_participants` 并设为 `editing`；用户可确认完成或重新开启编辑。
- 处理人确认完成方案后，参与者状态为 `confirmed`，不再显示主页入口角标或列表“待处理”；若审批人驳回其方案，才重新进入提醒状态。
- ECN 方案审批驳回必须选择一个或多个具体方案。所选方案标记为 `needs_improvement`（待改进）并记录当前驳回信息及历史；只有所选方案的作者转为 `needs_reconfirmation`（待重新确认），未被选中的方案及其他已确认处理人保持原状态。
- 作者修改被驳回方案后，方案状态转为 `revised_pending_confirmation`；存在尚未修改的被驳回方案时不允许重新确认。作者重新确认后，已整改方案转为 `revised_confirmed`，参与者恢复 `confirmed` 并停止提醒。
- 每次定向驳回只写入 `rejection_history`，并在当前记录保存排除审计/运行字段后的完整 `before_snapshot`；作者保存整改方案时，在同一记录补充 `after_snapshot`。快照覆盖项目、变更对象、要求/影响关联及变更前后内容，历史弹窗可逐次对照查看。

### 3.7 分阶段执行逻辑

方案评审全部通过时按三类方案一次生成当前结构的执行清单：

- 第一阶段由研发助理核对所有普通资料方案生成的事项清单，并单独勾选“ERP相关变更已执行完毕”。所有项目都确认后，研发助理点击按钮触发系统内资料方案执行。
- 系统内资料按方案、按项目记录 `pending / running / success / failed` 结果；界面逐条显示结果，失败时只重试失败项目，已成功项目不重复执行。
- 系统内资料任务重绘执行表格前会保留触发操作的客户端上下文，通知从客户端根容器发送，避免按钮随表格重绘被删除后继续引用失效插槽；“已开始执行”使用4秒自动关闭的信息提示，最终成功或失败另行提示。当前进程同时记录实际运行中的ECN；若数据库仍为 `running` 但进程内已无对应任务，执行页显示“恢复中断的执行”，按确定性节点ID继续处理未完成项目。
- 系统内资料全部成功后开放物料阶段；没有物料方案时直接关闭ECN。
- 第二阶段为每条物料方案按其 `traceability_levels` 生成追溯责任项。所有已选追溯范围彼此独立开放，不在范围之间设置先后关系；对应 `ecn:execution_*` 已发布流程控制范围内路线，具有相同执行阶段编号的负责人并行且分别确认，不同阶段依次串行，当前阶段全部完成后才开放下一阶段。旧料处置说明随每个追溯责任项展示并由该项负责人一并落实，不再单独生成确认项。单条方案全部追溯责任项完成后自动关闭，全部物料方案关闭后ECN自动进入“变更已完成”。
- `客户/在途` 使用特殊负责人：对方案关联的每个项目分别从 `app.storage.general["project_sale"]` 识别销售人员。“项目销售”和“销售主管”在执行流程中表示同一条逐级责任链，不能生成两个并行确认项；正常时只生成项目销售任务，未识别、离职或无权限时由同一任务逐级传给销售主管，再无法处理才继续传给销售总监。项目销售任务无论在哪一层上提，复选框均显示“项目 · 最终负责人（代确认）”，具体传导原因放在 tooltip；其它物料执行责任项沿直属上级上提时也使用“最终负责人（代确认）”，原岗位及上提原因由 tooltip 说明。
- 物料执行规格允许在界面只读推导，但不会因此修改本地 `execution_info`；真正勾选时，原子确认回调会先确保按当前配置生成的 `traceability_tasks` 已写入数据库，再从同一字典定位任务，避免出现“界面有复选框、提交却提示责任项不存在”。这样也避免本地临时任务与数据库持续不等、触发3秒协同定时器周期性重绘。确需因本人操作或其他用户更新而重绘时，系统保存并恢复执行页签容器的纵向位置及物料表的横向位置，而不是只恢复 `window` 滚动位置。
- 物料责任项的常规勾选/取消不再调用 `render_execution_tab()` 清空整个执行板块；页面保留各复选框、tooltip、行进度和状态徽标引用，只更新发生变化的当前物料方案控件。3秒协同轮询仍用于多用户近实时同步，但物料阶段未切换时仅把其他用户变化更新到对应控件；只有执行阶段改变、方案结构改变或最终完成关闭时才重绘整个执行区。
- 尚未关闭的物料方案允许原确认人取消自己的勾选；若同一追溯范围的后续串行节点已经有人确认，则禁止取消前序确认，避免形成逆序状态。全部物料责任项中的最后一个待确认项不能一次点击直接完成：界面弹出二次确认，原子回调也会重新统计数据库中的未确认任务进行并发兜底；用户确认后才真正保存最后一项并执行方案/ECN关闭处理。
- 最后一项二次确认弹窗打开前不再用 `set_value(False)` 主动复位复选框，避免程序性复位再次触发取消回调并误报“责任项尚未确认，无需取消”。用户取消弹窗或协同刷新使控件回到未选中时，若数据库本来就是未确认状态则静默同步；只有数据库确实为已确认状态的取消操作才进入取消权限和串行约束校验。
- ECN执行页统一使用表格化展示：第一阶段的普通资料事项与ERP共用“完成、编号、事项/方案、项目、执行前、应执行内容、确认记录”七列，项目号不混入方案标题；系统内资料另用结果表显示方案、项目、状态和说明，方案列同样不重复项目号。第二阶段每条物料方案固定只占一行，依次显示“编号、项目、变更类别、变更前、变更后、旧料处理方式、各追溯范围动态列、执行总状态”，项目、变更类别与结构化物料变更前后内容采用和ECN方案表一致的数据口径；每个追溯范围单元格内部按负责人节点组织复选框，存在多级串行时显示节点编号，确认人、角色、时间及前序等待状态放入复选框 tooltip。各表固定列对齐、使用轻量斑马纹，宽度不足时只在表格内部横向滚动。
- ECN执行页三张表的表头与内容列默认统一水平、垂直居中；统一对齐类同时使用 `content-center` 对齐内容区，并结合 Flex 方向处理单行与纵向组件组，避免NiceGUI多行标签或组件容器仅设置 `items-center` 时仍贴近单元格顶部。多行文本、旧料处置说明和追溯复选框组保持纵向排列并作为整体垂直居中。需要靠左的例外列集中维护在页面内 `execution_left_aligned_columns`，分别按 `assistant`、`overview`、`material` 三张表填写与表头完全一致的列名，动态追溯范围列也直接使用其配置名称；列改为靠左后仍保持垂直居中。

其中只有 `overview_update` 会自动修改各项目的 `<项目名>_over_data`：

- 更换：旧节点在当前需求版本失活并被标为禁用；创建新节点，继承旧节点的 `row_id`，写入新内容和 ECN 依据。
- 新增：创建新的概述 Chip；若是表格基准列则先生成 `row_id`，其他列再通过临时锚点绑定该行。
- 失效：只让选中的原概述节点失活，不创建对应新节点；多项目方案允许与新增、更换混合选择。
- ECN 自动落盘后的“最近操作人”统一记录该条方案的实际提供人；操作原因写入对应 `timestamp.reason`，ECN 编号写入 `timestamp.source_id`，不拼接顶层 `notes`。仅修改激活状态时不改变节点原始 `req_ver`；新建节点沿用正常概述录入规则，无项目需求时记录为“需求 V0.0 后”并生成 `{"0.0": true}` 激活状态。
- 执行器预先为基准列生成确定性 `row_id`，后续列通过临时锚点绑定该行；写入节点ID也由ECN、方案和项目确定性生成，使失败重试不会重复新增节点。
- 成功处理的自动方案标记 `execute_status=success`，失败方案保留具体项目结果和错误信息。
- `text_desc` 方案是人工执行记录，不会自动写入项目概述。

### 3.8 并发与刷新

- ECN 集合存储键：`ecn_management_data`，结构为 `{ecn_id: ecn_data}`。
- 所有 ECN 专属写入代理在成功后更新 `ecn_global_version_stamp`。
- 主列表每 5 秒只比较版本戳；变化后才重新读取和渲染列表，避免每轮遍历所有 ECN。
- 主列表不再用一单一卡片展示，改为可排序、筛选、调整列宽和分页的AG Grid。除ECN编号、当前状态、待我处理、变更简要、涉及项目、申请人与申请日期、流程进度外，按 `ECN_TRACEABILITY_LEVELS` 的JSON顺序动态增加各追溯范围总关闭状态列，并在总表最后固定增加“关闭日期”列；申请日期与关闭日期统一只显示年月日，未关闭记录显示“—”。“操作”、管理员“管理”和“ECN编号”固定在表格左侧。刷新按钮旁的“关注执行进度”开关开启后隐藏关注事项、简要、项目、申请人、申请日期及流程进度等中间列，保留当前状态、所有追溯范围列和关闭日期，以便在常用屏宽内完整查看后端执行状态；关闭开关即恢复中间列。一张ECN内同一范围的所有物料责任项统一汇总为“不涉及（—）/未开始/待确认/进行中 x/y/已关闭”。“详情”单击和行双击均可打开详情，管理员仍保留独立“管理/删除”列。
- 方案编写详情每 3 秒同步当前单据的工作流、方案参与者、方案条目和影响信息；执行详情同样每 3 秒同步其他责任人的勾选、自动执行结果和阶段推进。
- 影响信息采用基于编辑基线的字段三方合并；不同字段、不同扩展项目增删可并存，同字段不同修改拒绝并保留输入。轮询只同步未编辑字段，不覆盖正在输入的内容。
- 方案更新和删除以编辑窗口打开时的快照检测冲突，并在事务内复核作者和编写阶段；确认动作核对本人最新方案，未改内容的驳回方案不得直接标为已整改。
- 草稿保存/提交合并申请字段；审批、撤回和作废不写回客户端申请、方案或执行快照。新增 `approval_round` 区分不同提交轮次。
- 发起评审、同意、驳回在最新单据上统一校验状态、责任人和覆盖率。数据库模式单据 JSON 与 `work_assignments` 共用事务；SQL异常、业务拒绝及取消均回滚。
- ECN方案评审流程除原始ECR申请人的部门、岗位外，还可按“方案出具人所属部门/岗位”匹配。发起评审时从最新 `change_items[].author` 汇总全部方案作者，并按作者当前主部门、主岗位判断；任一作者同时符合所选部门与岗位即命中。申请人条件与方案作者条件同时配置时两组条件均须满足；通用兜底流程应使用更大的优先级数字。

### 3.9 ECN 角标提醒规则

- 数据库模式的审批角标按当前节点具体人员待办计算；同一会签节点中个人处理后会立即退出自己的待办，其他人的待办保留。旧 Excel 模式仍以 `pending_roles - step_approvals` 计算角色待办。草稿或 ECR 驳回仍只提醒申请人本人。
- ECR 审核通过进入方案编写后，若 ECN 影响区为空且没有具体处理人，只提醒拥有 `ecn.impact.initial_reminder` 的人员。
- 第一个用户在 ECN 影响区产生有效选择后，用户名写入 `workflow.impact_handlers`；其他用户后续参与处理时继续追加，支持多人负责。
- 一旦已有具体影响处理人，不再向所有研发/工程/质量角色泛化提醒，也不再额外提醒未参与的研发助理。
- 具体处理人在方案编写阶段仅当参与状态配置为需要提醒时显示角标：默认 `editing` 和 `needs_reconfirmation` 提醒，`confirmed` 不提醒。方案被定向驳回后，仅提醒被驳回方案的作者，直至完成修改并重新确认。
- 当方案参与者全部确认且覆盖完整时，只向拥有 `ecn.scheme.review.submit` 的人员提示“待发起评审”。第一阶段和失败重试提醒 `ecn.execution.assistant`；物料阶段逐条检查当前开放责任项及对应稳定执行权限，项目销售还按具体用户名过滤。
- 主页红色角标与 ECN 列表内“待处理”标签共用 `is_ecn_pending_for_user()`，保持统计口径一致。

### 3.10 ECN 企业微信待办提醒（2026-09-08）

- 2026-09-12 ECN通知改为企业微信文本卡片：固定模块标题和当前事件、灰色调试/抄送说明、转义并限制长度的待办摘要，底部查看详情打开ECN列表。沿用既有业务指纹，不因样式升级重发；每次业务重试重新生成卡片，不进入公共重试队列。未配置 `wecom.public_base_url` 时回退原文字提醒；其它模块仍用文字消息。
- 物料执行通知禁止展示方案 `item_id`、责任项 `key` 等内部编码，改用方案顺序编号、项目、变更类别、追溯范围和确认节点。正式个人提醒只包含该微信账号对应人员的待办；调试转发与研发经理观察抄送汇总本单全部实际待办人员，并完整列出人员姓名以便核对是否缺人。卡片待办最多预览3项，其余引导进入系统查看。
- ECN方案评审全部节点通过后，系统内置通知ECR及影响评审涉及项目的项目销售；同一销售负责多个项目时合并为一张卡片。销售账号停用、无法识别或未绑定企业微信时，沿直属上级逐级寻找第一个可送达层级；没有具体销售可追溯时从销售主管开始，再上提销售总监。该通知遵循ECN企业微信的调试转发和研发经理观察抄送开关，调试/抄送卡片完整展示原应通知人员。
- 客户端实测忽略卡片正文br换行，改为每项独立normal div；多人相同待办合并显示，仍列出接收人员。自动测试验证分块结构、转义及字节上限，实际客户端视觉仍需观察新消息。
- 后台入口 `src/modules/ecn/notifications.py`，由 `src/main.py` 启动；正式收件人复用 `ecn_access.is_ecn_pending_for_user()` 并要求在职且可以进入ECN，与首页可见角标一致。草稿与ECR驳回提醒申请人；审批提醒具体当前节点待办人员；方案阶段提醒未确认参与者、尚无参与者时的影响处理人，或影响空白时的初始提醒权限人员；全部确认且覆盖完整后提醒可发起方案评审人员；执行按当前助理阶段和开放物料责任项提醒。已完成/作废不产生新待办通知。
- 方案参与者列表非空时，影响处理人并不额外获得角标；以参与者状态为准。数据库模式的审批阶段优先按具体待办判断，不额外向所有编写人推送。
- 当前物料责任路线：文件→研发助理；供应商→采购后PMC；零件仓/半成品仓/成品仓→PMC；生产在线→生产经理后PMC；客户/在途→具体项目销售（缺失或不可处理时逐级传给销售主管、销售总监）后PMC。数据库模式对应稳定执行权限及具体用户名，旧Excel模式才匹配岗位名称。
- `ecn_management_config.json.wecom.enabled=true` 为通知总开关；`test_mode=true` 为默认调试转发，只发给 `test_notify_targets=[{"position":"研发经理"}]`，正文说明真实待办人员。开发完成后改 `test_mode=false` 并重启，按人员企业微信绑定发送；此开关不影响首页角标、权限和流程。调试收件人匹配失败不回退给正式处理人；正式人员未绑定也只记录并跳过。
- 默认启动30秒后、每60秒检查；同一待办24小时重复提醒，失败300秒后重新核对待办再重试。相关参数及列表跳转域名由JSON配置。通知状态独立保存在SQLite `ecn_wecom_notification_state`，成功去重按实际收件人记录；不向公共企业微信重试队列投递，避免已完成任务或切换调试路由后仍发送过期消息。
- `wecom.cc_manager_enabled=true` 默认开启正式模式研发经理观察抄送；每个ECN汇总已匹配正式账号的人员及待办，经理本人有待办时按微信账号合并一条。调试模式不额外抄送；未找到经理账号或解析失败不阻断正式提醒。观察完成设为false并重启，停止抄送及其失败重试；切换抄送不改变业务指纹，不触发正式人员重复提醒。抄送同样遵循重复间隔，重新开启时若经理近期已收到同一待办，则等待原间隔到期。
- `tests/test_ecn_notifications.py` 使用临时SQLite与模拟发送，覆盖抄送开关、账号合并、失败隔离与重试，不会触达真实企业微信用户。

### 3.11 特定事项独立移交与跟进（2026-09-14）

- 取消告知也使用事项/方案、项目、应执行内容三字段卡片，明确“已取消，无需继续处理”，不展示内部UUID。回收给操作人本人（指定本人或收回助理职责）时，在事项上记录本人通知豁免，后台不为该事项向本人发待办或生成其调试转发；原负责人取消告知、其他未完成事项以及页面角标/确认职责不受影响。再次移交他人时清除豁免。
- 移交卡片由 `special_task_messages.py` 优先显示执行表的事项/方案、项目、应执行内容，接收人及单号置后，不展示内部事项UUID或通用助理提示。三项字段分别分配摘要空间，防止长内容挤掉后续字段；多项时展示首项并注明剩余数量。取消告知使用独立文案，不套用当前待办摘要。
- 执行确认表每行（含ERP）增加移交负责人列，执行助理按部门、岗位筛选具体在职人员；接收人须有ECN查看权限，确认权依据最新单据中的具体用户名。已确认事项不能直接移交，变更负责人或确认均比较窗口/控件原始快照，避免并发覆盖。
- “执行系统内资料”按钮只要求未移交项确认；移交项独立跟进，可在后续系统内资料或物料阶段确认及调整负责人。启动系统内资料后不可收回为未移交项；物料和全部特定事项完成后才关闭ECN，保留未完成事项的角标及微信提醒。
- 总表“文件”前增加“特定事项”列，显示完成数及负责人提示，点击打开跟进与确认弹窗。后端服务为 `special_tasks.py`，界面为 `special_tasks_ui.py`。
- 修改已指定负责人时，取消告知事件与单据一起事务提交；`transfer_notifications.py` 后台发送原负责人取消告知，新接收人通过既有待办扫描通知。两者遵循现有调试转发和经理抄送配置，逐收件人独立去重/重试。通知发送不持有业务事务，未匹配账号按原规则跳过。
- `tests/test_ecn_special_tasks.py` 覆盖移交前置条件、指定人权限、过期页面、独立连接并发、删除后不复活、关闭条件、ERP、总表列及模拟微信取消/新待办/重试。测试不使用运行数据库或真实企业微信发送。

## 4. 当前已知风险与后续关注点

以下是静态阅读得到的工程风险，修改前应通过复现或测试确认：

1. ECN 已拆分到 `src/modules/ecn/`。新增功能按 README 的职责边界维护，避免重新把业务校验与持久化塞回页面。入口保留旧辅助函数导出供既有调用方使用。
2. 系统内资料逐条写项目概述，再更新 ECN 执行结果；并非所有项目写入的单一事务。已有确定性节点 ID 与幂等重试，异常中断恢复仍是独立关注点。
3. 编号已改为首次保存事务内分配，并发创建由独立数据库连接回归覆盖。
4. 重复的本地与数据库状态迁移实现已合并到 `actions.py`，权限/阶段/轮次/参与者确认/覆盖率检查基于事务读取的最新单据。
5. ECN 仍以 `general_storage` 中一个大 JSON 集合保存。单条更新仍会读写集合，数据量增大后再评估实体存储。
6. 新审批路径的 ECN 单据与身份待办已同事务提交；原有启动待办校准保留，用于修复外部维护或旧流程接口留下的不一致。
7. 方案详情轮询按结构化列表比较，规模扩大后可引入单据或方案修订号减少比较开销。
8. 新增 `tests/test_ecn_concurrency.py`，覆盖并发编号、三方合并、过期方案/草稿、评审竞争、真实身份数据库并行会签及事务回滚；实际概述跨项目执行的恢复仍需独立集成验证。
9. 上传文件使用原始文件名拼接到上传目录；需进一步核实同名覆盖、文件名净化、类型/大小限制和孤儿文件清理策略。
10. 执行责任路线的串并联拓扑仍由 ECN JSON 二维数组定义，数据库模式已将节点语义映射到稳定权限；若以后需要管理员直接增删执行节点，应为执行路线增加独立管理界面和稳定节点编码。
11. 方案关联区虽然标注“必填”，保存时只校验方案内容、项目和概述数据有效性；要求序号及关联资料/物料可为空。整体覆盖率能拦截遗漏的强制资料/物料，却不能保证每条方案都关联某条 ECR 要求。
12. 2026-09-02 已使用真实 Pyright CLI 清理完整 `src/`、`tests/` 的历史诊断：pandas 表格列统一通过 `pd.Index`、明确的 `Series`/`DataFrame` 收窄及分步聚合消除错误重载，内存 Excel 导出使用满足 `WriteExcelBuffer` 协议的缓冲区实现；全仓静态类型基线为 0 错误，后续改动不得重新引入诊断。

## 5. 像素数据统计分析工具

- 工具实现：`src/tools/pixel_statistics.py`；入口位于分析工具页，权限键为 `pixel_statistics`。
- 处理流程：上传 Excel/CSV -> 数值校验 -> 可选颗粒度分块平均 -> 中心定位 -> 区域筛选 -> 统计 -> 预览及 Excel 导出。CSV 支持 UTF-8、UTF-8 BOM、GBK/GB18030 及常见分隔符自动识别，并按单工作表处理。
- 中心算法包括数据几何中心、全局最大值位置、手工坐标，以及“最大值百分比阈值区域中心”。阈值算法只使用包含全局最大点且面积最大的 8 邻域连通区域，并以区域几何质心为中心，避免远处孤立亮点拉偏。
- 颗粒度合并按实际矩阵居中裁剪，可选让合并后行列数保持奇数；固定尺寸模式会严格拒绝尺寸不匹配的工作表。
- 矩形统计范围严格选择指定数量的行列，不沿用旧脚本包含边界而多算一行一列的口径。毫米范围按处理后比例尺换算后必须完整落在数据表内；矩形或圆形超界时直接报错，不再裁小为表内残缺区域继续统计。
- 比例尺以“原始像素点数对应多少毫米”配置，两个输入均支持最多 6 位小数；圆形半径及矩形长宽按毫米输入。启用颗粒度合并后，系统会按颗粒度自动换算处理后网格的像素密度，全程保留浮点比例尺，不先取整。
- 使用最大值百分比区域中心时，结果区额外绘制主连通区域边缘和几何中心示意图。坐标轴显示整张处理后数据的毫米范围，绘图区按数据物理长宽比设置，保证横纵方向毫米单位 1:1 显示。边缘蓝点是达到阈值的主连通区域的离散边界像素，点值满足但不一定等于判定阈值；悬停提示同时显示 X、Y、边缘点数值和判定阈值。完整区域用于中心计算，示意图每张最多抽取约 800 个边缘点；6 张图以内一次生成，超过 6 张时先生成 6 张并在滚动到底部时每次追加一行（2 张），宽屏始终每行两张。
- 工具界面将比例尺、中心、统计范围、矩阵均匀性和颗粒度作为常用设置；宽屏采用五列等宽、等高卡片，窄屏响应式降为两列或一列。尺寸校验、异常单元格及奇数网格放入默认收起的高级设置。统计结果默认只显示最终指标，处理尺寸、比例尺、定位与裁剪信息放入折叠明细；常规、矩阵和明细表的表头及单元格统一居中。
- 上传区和常用设置在宽屏下采用左、右双栏布局，窄屏自动恢复上下排列；阈值边缘与中心示意图板块默认收起。
- 宽屏双栏由常用设置区决定高度；上传文件标签超出该高度后只在上传卡片内滚动，不再把整行布局撑高。
- 上传器隐藏内部文件列表和 Quasar 原生累计上传大小，使用下方文件标签作为唯一文件状态和删除入口；当前文件数与总大小由服务端现有文件列表实时汇总，删除标签时同步清理上传器队列和服务端分析数据并立即扣减总大小。
- 非圆形统计区域可启用矩阵均匀性统计：把当前全域或矩形区域按用户指定的竖向×横向数量等分，在每格中心按毫米边长取正方形样本并先求均值，再对这些格均值计算常规指标。页面及汇总字段均以“矩阵”前缀区分，Excel 另附每个工作表的采样矩阵。
- 结果不再显示或导出总体标准差、样本标准差，只保留相对总体标准差和相对样本标准差。标准差仅作为计算相对指标的内部中间量。
- 常规统计和矩阵均匀性统计均包含“极值最大偏差/平均值”指标，公式为 `max(|最大值-平均值|, |最小值-平均值|) / 平均值`；平均值为 0 时结果按不可定义处理，页面显示“—”，Excel 写为空值。
- 统计结果表的公式列在表头显示帮助图标；鼠标悬停时展示公式定义及除零、样本数不足等不可定义条件。常规与矩阵公式说明由同一字段映射集中维护。
- 导出文件包含统计汇总、处理参数和矩阵均匀性采样工作表；启用颗粒度合并时还包含合并后的数据矩阵工作表。

### 照度/辐照度转强度分布工具（2026-09-14）

- 实现：`src/tools/intensity_distribution.py`，入口在 `src/pages/tool.py`；独立权限 `tools.intensity_distribution.use` 已注册于权限目录。数据库模式需要管理员给相应岗位/权限组授予新工具权限；旧 Excel 角色模式在 `tools_permission.json` 采用与光斑均匀性相同的允许角色。
- 用户确认数据来自垂直于“光源—所选中心”轴线的平面；所选中心作为光源在测量平面上的垂足。用户必须填写垂直测量距离（mm），并在“数据值到 lx”与“数据值到 mW/cm²”两个系数中且只填写一个；界面输入任一系数会清空另一个。支持小数比例尺、Excel/CSV 多表上传和四种中心定位算法，默认采用 10% 的“最大值百分比区域中心”。
- 光度模式：`E_v(lx) = 表格值 × lx系数`，输出光强 `cd`。辐射度模式：`E_e(mW/m²) = 表格值 × mW/cm²系数 × 10000`，输出辐射强度 `mW/sr`。两种模式均先把距离转为米，再按 `I = E × (d²+x²+y²)^(3/2) / d = E × d² / cos³θ` 换算。该逆平方及余弦换算要求光源可近似为点光源；近场平面照度/辐照度不等同于完整远场配光测量。
- 亚像素中心用双线性插值求中心照度；水平/垂直过中心剖面先插值照度，再按剖面实际角度换算强度。强度矩阵和完整剖面首次计算后缓存；图表区提供两个默认 50%/10% 的目标比例输入，修改时只按“中心强度 × 百分比”即时重算四侧首次下降交点，不重新读取表格或换算矩阵。任一输入为空或 0 表示停用该目标，不计算、不绘图也不导出；负数、非有限值及大于 100 的值拒绝。在相邻角度/强度样本间线性插值；100% 取 0°，未覆盖交点显示“未找到”，不跨边界外推。实时角度表和 Excel 角度汇总只包含启用的比例。
- 可选在定位和换算前按 `N×N` 原始单元格求块平均；不能整除的边缘按像素统计工具的相同规则居中裁剪。合并后的比例尺自动除以 `N`，后续中心定位、光强矩阵、剖面、交点和导出全部基于平滑后的网格；手工中心启用时填写合并后网格的行列坐标。处理汇总同时保留原始尺寸、处理后尺寸和裁剪说明。
- 每份表格的笛卡尔图和极坐标图并排显示；极坐标仅绘制 `-90°～90°` 上半圆，并利用 ECharts `angleAxis.endAngle` 与约两倍半径充分利用卡片区域。强度单位移到图内顶部，半径刻度减少并启用标签避让，角度交点标签按正负方向及水平/垂直方向错位，降低重叠。两图可同步切换绝对值或“中心=100%”归一化显示，并共用“按角度找强度”和“按强度找角度”悬停开关：前者在同一角度对水平/垂直曲线插值，最多显示 2 个圆点；后者以鼠标强度为目标，从每条曲线中心向负角、正角查找首次交点，最多显示 4 个圆点。笛卡尔图用直线连接点与坐标轴；极坐标图按角度查询时显示径向辅助线，按强度查询时显示等强度半圆弧及各交点径向线。两个图的客户端悬浮框均列出全部实际交点坐标，不再使用 ECharts 默认的单系列高亮点。水平、垂直曲线各自保留独立图例；每个目标比例的强度线、两方向角度交点标签和全部角度虚线归入各自目标图例。纯客户端图例联动还会在隐藏某方向曲线时同步隐藏该方向在所有目标比例下的交点、数字与角度虚线。通过工作表选择器切换两张图，图形单剖面约 800 点，计算和导出保留完整数据。
- 上传复用 `custom_upload()`，移除/清空同步清除文件和旧结果；文件/参数变动通过版本号防止旧后台结果回填。导出包含角度汇总、参数、错误、带实际单位的完整强度矩阵及水平/垂直角度剖面。
- 回归覆盖已知恒定光强的平面照度反算、亚像素中心、非对称交点、多波峰首次交点、零中心/非法输入、单位换算、批处理、导出和 UI 刷新绑定。

## 6. 用户身份与组织架构重构

- 旧用户源仍为 `data/users.xlsx`。新代码部署后若 `iam_users` 为空，`UserService` 继续读取旧 Excel，避免服务器在尚未迁移时失去登录入口。
- 管理员可在 `/manage` 执行“一键迁移用户到身份数据库”，或在项目根目录运行 `python scripts/migrate_users_to_iam.py`。迁移读取执行机器上当时的 Excel，因此本地统一测试密码不会覆盖服务器自己的密码。
- 普通迁移是幂等的：新增缺少的用户、同步兼容角色，但保留数据库中已有密码哈希。只有显式执行 `--refresh-existing-passwords` 才重新以 Excel 密码覆盖已迁移密码。
- 迁移不写回或删除 Excel，并在 `backups/user_migration` 保存源文件快照。迁移成功后登录改用 PBKDF2-SHA256 哈希验证，管理页面不再显示或回填原密码。
- 身份和组织关系表位于现有 `db/nicegui_storage.db`，使用 `iam_*`、`org_*` 和 `work_assignments` 表名，与既有通用 JSON 表共存。核心实体包括稳定 `user_id`、部门树、岗位、直属上级、多对多权限角色、企业微信外部身份和具体人员任务。
- `/manage` 通过单一“用户、组织与权限中心”入口汇总用户账号、组织岗位、岗位权限、附加权限、企业微信通讯录、身份迁移和运行数据刷新；原先分散排列的六个管理入口不再直接显示。中心内提供用户停用/恢复、企业微信一对一手工绑定、企业微信部门/职务主动导入、部门/岗位字典维护及用户主部门/岗位/直属上级分配。系统组织主数据独立于企业微信：新导入记录初始跟随企业微信，管理员手工编辑后自动进入本地保护，后续重复导入只刷新外部快照而不覆盖本地名称、部门层级或岗位名称；纯手工记录始终不受导入影响。企业微信职务只有自由文本而无稳定岗位ID，外部职务改名会作为新的岗位候选导入，不自动猜测合并。
- 岗位与部门通过 `org_position_scopes` 建立多对多适用范围：同一岗位可以供多个部门共用一套默认权限，需要不同权限时应建立不同稳定编码的岗位。岗位字典和岗位默认权限列表均按部门树的父子层级递归分组，父部门会包含下级部门折叠节点；所有部门及“未划分部门”分组首次进入时默认收起并可手动展开，同一弹窗内局部刷新会保留用户已经展开的部门。岗位编辑可多选适用部门。用户任职表单先选主部门，岗位下拉只显示该部门岗位，后端也会拒绝跨部门岗位组合。岗位适用范围不能直接移除仍有在职员工任职的部门；应先暂时保留新旧部门、把相关员工主部门迁到新部门，再移除旧部门，岗位表单的警告提示会说明这一顺序。升级时以现有在职任职关系自动补齐历史岗位范围；首次把尚未归类的历史岗位分配给用户时也会建立对应范围。企业微信职务导入先同步部门，并按成员所属部门累加岗位适用范围；进入本地编辑保护的岗位不再由重复导入修改名称或部门范围。
- 用户管理支持企业微信安全自动匹配预览：优先匹配“系统用户名=企业微信 userid”，其次匹配唯一同名成员；重名、多个系统用户争用同一微信账号、停用成员和无法匹配记录只进入人工处理，不自动绑定。手工绑定和应用自动匹配均不会再隐式导入部门或岗位字典，只会使用管理员已经明确勾选导入的字典补齐尚未设置的系统任职；已有系统任职不被覆盖，直属上级因通讯录缓存没有可靠的人员上下级字段仍需人工确认。
- 企业微信部门和岗位字典均采用“预览勾选后导入”：两个导入按钮先展示通讯录缓存候选项且默认不选择，支持全选和清空，只有管理员明确勾选的记录才提交。部门候选按企业微信父子层级展示；选择下级部门时，其上级必须已经存在于系统或在同一批次选中。岗位候选按职务名称汇总人数和所属部门，岗位导入不会暗中导入未选择的部门；所属企业微信部门尚未导入时，该岗位暂归入“未划分部门”并明确提示。
- 稳定权限目录集中在 `src/permission_catalog.py`，统一判断入口在 `src/access_control.py`。权限管理以岗位为主要载体：`iam_position_permissions` 保存岗位默认权限，拥有该主岗位的在职用户自动继承；“附加权限组”只处理兼职、专项职责和特殊管理权限，一个用户可叠加多个权限组。岗位权限、权限组与用户附加授权变更均写入 `iam_audit_logs`。权限管理界面按业务模块折叠，并在模块内用表格继续划分子分类；项目概述的同一 `label` 把“查看、维护”合并为一行，避免大量权限平铺混杂。
- 企业微信职务文本不会直接授予任何权限；管理员需先确认或维护对应的系统岗位，再在“岗位默认权限”中授权。旧角色关系继续在后台承接未完成迁移的模块，但不再作为日常权限管理的主入口。
- 手工新建部门、岗位和附加权限组统一使用稳定编码：去除首尾空白后转为小写，长度 3–64 位，以英文字母开头，只允许字母、数字、点、下划线和横线。三类编码分别在各自字典内不区分大小写地唯一；管理表单输入时实时校验格式和查重，保存时后端再次校验。企业微信既有部门内部编码 `wecom:数字` 作为历史系统编码保留且编辑时锁定，不要求按手工规则重写。
- 权限目录初始化及旧角色默认授权是幂等的。`tools_permission.json` 只在首次建立对应“兼容角色—权限”关系时提供精确名称映射；管理员之后手工移除的权限不会被重启或重复迁移重新添加。
- 岗位默认权限和附加权限组均采用自动保存，不再提供单独保存按钮。岗位权限勾选、附加权限组名称、启停状态或权限勾选发生变化时立即保存；名称输入使用短暂防抖。保存成功只刷新左侧摘要，保持右侧滚动位置，保存失败时提示并重新读取数据库中的真实状态。岗位默认权限支持从另一个岗位一次性完整复制：复制会覆盖目标岗位当前权限，但不改变岗位资料、部门范围或用户任职，完成后两个岗位的权限仍可独立修改。
- 岗位字典支持删除无人任职的岗位。系统以“有效主岗位”作为当前占用口径：用户更换或清空主岗位时，旧任职关系会结束，不再继续计入岗位人数或阻止删除；删除操作在数据库事务内再次检查当前主岗位，仍有用户使用时拒绝并提示用户。确认无人任职后同步清理遗留历史引用、岗位默认权限和部门适用范围，删除动作写入身份审计日志。企业微信通讯录中仍存在的同名职务在以后重新导入时可能再次生成岗位。
- 用户管理提供独立的永久删除按钮，用于清除误建账号；旧 Excel 模式删除工作簿中的账号行，身份数据库模式在单个事务中清理该用户的任职、微信绑定及角色关系，并记录删除审计。`admin` 账号禁止删除；已有业务待办、本人操作审计记录或作为其他员工直属上级的账号不能永久删除，应先处理关联或使用停用。删除后可重新创建同名账号。
- 用户管理列表按主任职所在部门的父子层级分组，同一用户只出现在一个部门；没有有效主部门的员工归入“未划分部门”，`admin` 单独归入“系统账号”。部门标题人数包含本部门及全部下级部门的用户，搜索时统计当前命中用户。仅展示含用户的分支，根部门初始展开，手动展开状态在列表刷新后保留。搜索用户名、部门路径、岗位、兼容角色或企业微信资料时只展示命中用户，并自动展开相应部门；旧 Excel 模式中的员工统一归入“未划分部门”。`boss` 可不设直属上级，列表明确显示“无需设置”；未保存系统主任职的账号明确显示“主部门待设置”。
- 管理中心提供“配置迁移”：本地导出 UTF-8 JSON，服务器上传后先检查文件校验值、稳定编码、权限目录、流程引用、用户名和企业微信绑定冲突，只有无硬错误时才能确认导入。配置包包含部门树、岗位及适用部门、岗位默认权限、非兼容附加权限组、按用户名表达的当前任职/附加授权/外部账号、审批流程当前发布版与草稿，以及概述权限整理辅助映射；明确排除密码、用户基本资料、业务单据、具体待办和审计日志。导入按目标环境重新解析稳定编码和用户名，不复用本地随机 ID；同编码配置合并、服务器独有配置保留、变化的已发布流程生成新版本。正式写入前自动备份至 `backups/config_import`，全部配置在单个事务内完成，失败整体回滚。本地测试密码不会进入配置包，服务器已有密码哈希始终保留。
- 分析工具页是首个迁移业务模块：数据库模式只按 `tools.<tool_key>.use` 权限编码判断，旧 Excel 模式继续精确读取原角色列表作为部署兼容，因此服务器不先迁移也不会改变现有工具权限。系统管理入口同时接入 `system.manage`；`admin` 用户名暂时保留为分阶段上线期间的紧急管理入口，避免误配权限导致管理端锁死。
- 样品单执行看板是首个细粒度迁移的业务看板。数据库模式分别按 `sample_order.view`、`sample_order.base.edit`、`sample_order.delay.edit`、`sample_order.special_status.edit`、`sample_order.delay_nature.edit`、`sample_order.delete` 和 `sample_order.average_score.view` 控制模块入口、基础/执行维护、延期维护、特殊状态、延期性质、删除和平均考核分查看；主页入口、页面直达和保存回调都执行权限检查，兼容角色不再授予这些权限。旧 Excel 模式仍读取原角色配置与模块入口关键词，服务器在身份迁移前保持原行为。记录中的创建/更新角色文本作为历史留痕继续保存，不参与数据库模式授权计算。
- 样品单主页红色角标不设置独立权限，而是合并当前用户有权处理的业务待办：拥有延期维护权限时计入逾期未交样订单，拥有延期性质权限时计入已交样但尚未标记性质的延期订单，同时拥有时按订单去重合并。点击角标进入 `view=my_pending`，看板自动启用“我的待办”筛选；没有任何对应处理权限的用户不显示该筛选项。
- 异常单跟进模块已接入 `error.view`、`error.record.edit`、`error.request.approve`、`error.record.rename`、`error.record.delete` 和 `error.reminder.check`，分别控制入口、整单维护、延期/关闭申请审批、改号、删除和人工提醒检查。数据库模式不再读取兼容角色授权；旧 Excel 模式仍按原编辑角色、审批角色及入口关键词运行。负责人本人处理措施仍按具体负责人字段判断，申请人/审批人角色文本继续作为历史快照保存。主页异常角标点击后进入“我的待办”筛选，筛选复用负责人及申请审批判断。
- 固定岗位通知接收人也已按业务事件迁入稳定权限。权限管理界面按“样品单执行看板 · 通知接收”“异常单跟进 · 通知接收”和“样品问题跟进 · 通知接收”分别显示，不把不同业务模块的通知混在同一分组。样品单拆为“延期关注、特殊状态”；异常措施拆为“延期申请、延期审批结果、延期通过追加、关闭申请、关闭审批结果、关闭通过追加、无负责人兜底提醒”；样品问题拆为“延期申请、延期审批结果、延期通过追加、默认关闭申请/结果/通过追加、研发电子类关闭申请/结果/通过追加、无人员兜底提醒”。数据库模式按“在职用户拥有通知权限 + 已绑定企业微信账号”解析收件人，旧 JSON 中的职位/角色目标只供服务器尚未迁移的 Excel 模式兼容；申请人、负责人等随单据变化的动态人员仍按具体姓名及企业微信绑定发送。`admin` 的自动全权限不代表自动订阅全部通知，因此固定通知解析默认排除系统管理员账号。上一版宽粒度通知权限会在权限目录同步时把已有岗位及附加权限组授权复制到对应细权限，然后删除旧权限，且不会在后续启动时覆盖管理员对细权限的调整。
- 在职 `admin` 系统账号自动拥有权限目录中全部已注册权限，不依赖主部门、岗位、直属上级或附加权限组；无效或拼写错误的权限编码不会因管理员身份被放行。管理员可为 `admin` 单独设置企业微信通知地址；未设置时用户列表明确显示待补项。该地址保存在 `iam_meta`，可以与员工的一对一身份绑定指向同一企业微信成员，且不占用该成员的员工绑定名额。员工之间仍严格保持一对一；admin 地址只供以 admin 为具体收件人的通知解析，固定通知权限仍默认排除 admin，以免自动全权限导致其订阅所有通知。管理员通知地址属于当前部署的本地设置，不随身份配置包迁移。
- 样品问题跟进模块已接入 `sample_issue.view`、`sample_issue.record.create`、`sample_issue.record.edit_all`、`sample_issue.extension.approve`、`sample_issue.close.approve`、人工提醒检查和删除权限。创建人维护录入区块、当前负责人维护对策及具体特殊准备负责人等随单据变化的责任规则继续按具体人员判断；页面直达、按钮、保存和审批原子更新均复核权限。主页角标点击进入 `view=my_pending`，列表自动筛选当前用户负责或有权审批的记录。关闭审批只保留一项稳定资格权限；默认组和研发电子组的实际审批人由同一业务事件下不同的流程条件、审批人规则及具体待办区分，不再为每条流程复制审批权限。
- 项目资料总表已接入 `project.view`、`project.all_states.view`、`project.base.edit`、`project.status.edit` 和 `project.engineer.assign_all`。数据库模式只有拥有查看权限的用户才能看到主页卡片或直接进入页面；拥有查看权限但没有全部状态权限时，只显示“试产、量产”项目。基础资料维护只控制新增项目及立项日期、简介、备注和客户简称；项目状态修改、为全部项目指定项目工程师分别独立授权。没有状态权限但有基础资料权限时，新项目固定使用默认“研发”状态，修改既有项目时后端保留原状态。上一版 `project.record.manage` 授权会一次性拆成上述三项维护权限后删除，管理员之后可分别收回且不会被重启重新添加。旧 Excel 模式仍兼容原角色行为。
- 项目需求正文已接入六项 `project_requirement.*` 稳定权限：查看，维护，本人负责项目审批，全部项目审批，撤销已通过审批，以及查看全部用户草稿。项目资料总表跳转、需求页面直达、文件路径边界、上传、新建、暂存、自动保存和提交均复核权限；跨用户草稿只能只读打开且仍只有创建人能删除。需求审批采用“稳定审批资格 + 项目基础资料中明确指定的项目工程师”双重判断；拥有“本人负责项目需求审批”但未被当前项目指定的人不能代审，“全部项目需求审批”则不受项目分配限制。主页待办角标和项目待办页使用同一规则。旧 Excel 模式继续兼容销售维护、研发经理全局审批/撤销、具体项目工程师审批及研发经理查看全部草稿。
- 项目待办工作台使用 `project_todo.view` 作为独立入口权限；主页卡片和 `/information` 页面直达使用同一校验。入口权限只允许进入聚合工作台，不会隐含需求维护、需求审批、概述维护或概述审批能力，各区块继续调用对应业务权限和具体待办判断。本人概述待办及主页角标还会按当前配置逐个复核 `project_overview.item.<label>.edit`，已经被指定为负责人但没有该项维护权限时不展示不可处理待办。旧 Excel 模式继续兼容原销售、研发、工程、质量、boss 和 admin 角色关键词。
- 需求项结构是纯查看模块，统一使用 `question_tree.view`。主页卡片、`/question_tree_tabs` 结构浏览页和 `/print_list` 完整打印清单均独立复核同一权限，打印页不能绕过主页或主页面直接读取清单。数据库模式不再读取岗位名称；旧 Excel 模式继续兼容原销售、研发、boss 和 admin 角色关键词。
- 生产测试项汇总表统一使用 `project_test_summary.view`。无权限时，项目资料总表不生成“测试项”列，项目概述页不生成“测试项”按钮；点击回调会实时复核权限，`/report/test_summary/{project_name}` 报表路由也会独立校验，不能通过手工拼接地址绕过。查看后使用浏览器打印不会形成新的服务端数据范围，因此不单独设置打印权限。旧 Excel 模式继续保持原有登录用户均可查看的行为。
- 项目概述按 `overview_config.json` 中每个稳定 `label` 分别设置查看和维护权限，编码为 `project_overview.item.<label>.view/edit`；维护权限自动包含同一项的查看。权限目录会在系统启动、打开权限管理和手动刷新概述配置时从当前 JSON 动态生成，新加 `label` 自动出现在管理界面，改标题只更新显示名称且保留既有授权。删除的 `label` 会从管理界面隐藏，但数据库中的旧授权暂留以便恢复和审计；重复或不符合格式的 `label` 会阻止刷新并明确报错。上一版六个专业维度的授权会一次性展开到该专业当时已有的全部 `label`，随后删除旧专业级权限。失活内容查阅、直接修改全部概述原始内容、批量概述变更申请、批量变更审批及原记录纠错审批另行授权。项目总表概述跳转、概述页面直达、分组/条目渲染、两个概述控件的保存和纠错入口均复核稳定权限；概述页上方的项目状态和项目工程师操作分别使用 `project.status.edit`、`project.engineer.assign_all`。`overview_charge_pending` 已按具体用户名显示，不再套一层模块角色名单。数据库模式下批量变更和原记录纠错分别使用 `project_overview:batch_change`、`project_overview:correction` 业务事件，新申请必须命中已发布流程并保存具体审批人快照；旧 JSON 路由可在管理页生成待检查草稿但不会自动发布。旧 Excel 模式继续读取概述配置中的 `read_role`、`edit_role` 和 `overview_change_workflow_config.json`。
- `updata_overview_config()` 的配置同步逻辑与 NiceGUI 通知已经解耦。系统 `master_startup` 在没有浏览器客户端和 UI slot 的后台上下文中使用 `show_notification=False`，只写日志；管理页面手工刷新仍显示成功或失败通知。即使页面提前关闭导致通知上下文失效，也不会把已经成功完成的配置与权限目录同步反向标记成失败。
- 岗位默认权限页提供“概述权限批量映射”。管理员可把旧 `overview_config.json` 的一个角色值（例如“质量”）映射到一个或多个系统岗位；系统统计该旧角色覆盖的查看/维护 `label`，把多个旧角色命中的权限按岗位取并集，并在应用前预览每个岗位将新增和移除的数量。确认后只调整本工具上次自动生成及本次准备生成的 `project_overview.item.*` 权限，管理员另外手工补充的概述权限以及其他模块、审批和通知权限保持不变；取消既有映射并再次整理只会清除对应岗位原先由该工具自动生成的概述项权限。映射配置、自动权限快照和岗位权限在身份数据库同一事务中保存，可安全重复执行，操作写入身份审计日志。
- 数据库模式下，分析工具权限 `tools.*.use`、系统管理权限 `system.manage`、项目资料权限 `project.*`、项目需求权限 `project_requirement.*`、项目待办权限 `project_todo.*`、需求项结构权限 `question_tree.*`、生产测试项汇总权限 `project_test_summary.*`、项目概述权限 `project_overview.*`、样品单权限 `sample_order.*`、异常单权限 `error.*`、样品问题权限 `sample_issue.*`、设计知识库权限 `design_knowledge.*`、统计权限 `statistics.*`、ECN权限 `ecn.*` 和通知订阅权限 `notifications.*` 已正式停止计算 `legacy.*` 兼容角色授权，只合并主岗位默认权限、非兼容附加权限组和 `admin` 自动权限。旧兼容授权关系暂留数据库供审计；服务器尚未迁移、仍采用 Excel 用户源时继续按旧角色配置判断，避免部署代码本身造成生产失权。
- 现阶段 `legacy_role/current_role` 仍保留用于其余未迁移业务模块兼容；后续按模块将关键词/精确角色判断替换为权限点、组织审批策略和具体 `assignee_user_id`。
- 旧兼容分支已增加统一运行监测。只有旧 Excel 身份源实际加载或认证成功、旧角色规则实际授予权限、旧 JSON 通知/审批路由实际采用，以及数据库中迁移前待审记录确实按历史角色快照收尾时，才写入带 `LEGACY_COMPAT_HIT` 标记的警告日志；权限拒绝和数据库稳定权限正常命中不记录。同一“类别 + 功能 + 用户”首次立即记录，之后默认三十分钟汇总一次被抑制的重复命中，可用环境变量 `LEGACY_COMPAT_LOG_INTERVAL_SECONDS` 调整限频窗口。该日志用于确认兼容代码是否仍有真实业务依赖，不能仅因短期未出现日志就直接删除旧配置。
- 通用审批流程引擎已落地到身份数据库：`approval_workflows` 保存稳定流程编码和启停状态，`approval_workflow_versions` 保存不可变的发布版本、匹配条件、审批人规则、所需权限和通知设置，`work_assignments` 保存业务申请创建时解析出的具体人员待办。管理中心的“审批流程”页签支持草稿、发布、停用、版本迭代及按申请人模拟匹配；草稿不会影响业务，发布版本只影响发布后新建的申请，历史单据继续使用随单固化的版本和审批人快照。
- 每个流程版本可配置“全部节点通过后额外抄送岗位”。这是申请人、审批人、责任人及模块既有通知之外的附加收件人；岗位以稳定 ID 保存，最终完成时按当前在职主任职和企业微信绑定动态解析，重复账号合并。配置随单据审批快照固定，中间节点、驳回和未启用配置不触发。管理界面同时预览可送达人员、未绑定人员和空岗位；流程配置导入导出按岗位稳定编码转换。样品问题关闭、设计知识、项目概述、ECN 的 ECR/方案评审以及七类执行责任路线均已接入；执行路线在同一物料方案、追溯范围和项目下的全部责任节点确认后触发一次。异常单和样品问题 JSON 中旧 `approval_notify_targets` 已删除，原数据库通知权限、申请人通知、待办及定时提醒保持原逻辑。
- 流程引擎核心已经支持“多节点串行 + 节点内任意一人/全部会签”：流程版本在 `approver_json.nodes` 中保存不可变节点列表，每个节点拥有稳定节点编码、名称、审批人来源、所需权限和节点审批方式；旧版本会自动视为单节点。创建流程实例时一次解析并固化所有节点的具体审批人快照，只激活首节点待办；任意一人节点在一人通过后结束同节点其余待办并进入下一节点，全部会签节点必须所有具体审批人完成后才推进。相同优先级流程仍拒绝静默选择，`admin` 也不会仅凭管理员身份成为具体审批人。管理页面已经开放单节点/多节点模式切换、节点新增删除与排序、逐节点审批人和任意/会签配置，并能在模拟结果中按顺序展示节点；目前仅 `ecn:ecr_review` 与 `ecn:scheme_review` 明确接入多节点运行接口并允许发布多节点版本，其余事件的多节点配置仍只允许保存草稿。
- 样品问题关闭申请是首个接入通用流程引擎的业务事件，事件键为 `sample_issue:close_request`。数据库模式的新申请必须命中一个已发布启用的流程；没有流程、没有匹配条件或解析不到合格审批人时会拒绝提交并提示管理员配置，不再回退到岗位名称或 JSON 角色关键字。提交成功时把流程编码、版本、统一审批权限 `sample_issue.close.approve` 和具体审批用户名写入关闭申请，并创建 `work_assignments` 待办；审批时同时校验统一资格权限与本单具体待办。旧“默认关闭审批”和“研发电子类关闭审批”的岗位及附加权限组授权会在权限目录同步时合并到统一权限，已有流程草稿/发布版本中的权限引用也同步替换；迁移前已经待审且没有流程待办的记录仍可按原审批角色快照收尾。管理页可把旧 JSON 路由转换为待检查的草稿，但不会自动发布。默认组与研发电子组的通知订阅仍是独立权限，这一拆分只影响固定消息接收人，不参与审批资格判断。
- 设计知识库已迁移为七项稳定权限：查看、录入、维护本人知识、知识发布审批、受控标签库维护、新标签审批和删除。主页入口、页面直达、表单保存、状态变更、标签维护及删除回调均复核稳定权限；主页角标合并本人被退回知识、具体分配的知识审核和标签审核待办。审批流程新增 `design_knowledge:knowledge_review` 与 `design_knowledge:tag_review` 两个业务事件，数据库模式的新提交必须命中已发布流程并固化具体审批人，只有同时拥有对应审批权限且持有本单待办的人员可以处理；拥有相同审批权限但未被分配者不能代审。迁移前已经待审核且没有流程快照的记录仍可按原固化角色完成收尾，旧 Excel 模式继续读取 JSON 角色规则。管理页可分别把样品问题和设计知识旧路由生成草稿，导入结果不会自动发布。

### 6.1 当前已整理迁移的模块总览

这里的“已迁移”特指：身份数据库模式下，模块入口和关键业务操作已经使用稳定权限编码，
不再把 `current_role`、旧安全角色名称或角色关键词当作运行时授权来源。旧 Excel 模式仍保留
兼容规则，服务器可以先部署代码、完成验证后再执行用户迁移。

| 模块 | 当前状态 | 稳定权限或流程 | 主要运行规则 |
| --- | --- | --- | --- |
| 用户身份、组织和系统管理 | 已迁移 | `system.manage` | 用户、部门、岗位、直属上级、附加权限组、企业微信绑定和审批流程统一在身份数据库维护；在职 `admin` 自动拥有已注册功能权限。 |
| 分析工具入口 | 已迁移 | `tools.<tool_key>.use` | 数据库模式逐个工具授权；各计算工具内部算法不参与身份判断。 |
| 项目资料总表 | 已迁移 | `project.view`、`project.all_states.view`、`project.base.edit`、`project.status.edit`、`project.engineer.assign_all` | 查看入口、全部状态范围、基础资料、状态修改和全部项目的项目工程师指定分别授权。 |
| 项目需求正文 | 已迁移 | `project_requirement.*` | 查看、维护、本人负责项目审批、全部项目审批、审批撤销和跨用户草稿查看分别授权；本人负责审批还必须匹配项目上具体设置的项目工程师。 |
| 项目待办 | 已迁移 | `project_todo.view` + 各业务模块权限 | 独立控制聚合工作台入口；内部需求、概述和审批区块不扩权，概述角标只统计本人有权维护的 `label`。 |
| 需求项结构 | 已迁移 | `question_tree.view` | 主页入口、层级结构搜索浏览和完整打印清单统一授权；模块没有业务数据写操作。 |
| 生产测试项汇总 | 已迁移 | `project_test_summary.view` | 项目资料列、项目概述按钮、点击回调及报表直达统一授权；查看权限包含浏览器打印。 |
| 项目概述 | 已迁移 | `project_overview.item.<label>.view/edit`、`project_overview:batch_change`、`project_overview:correction` | 每个 JSON `label` 独立控制查看/维护且动态进入权限目录；批量变更和原记录纠错按已发布流程固化具体审批人，旧角色配置仅供 Excel 模式和草稿导入。 |
| 样品订单看板 | 已迁移 | `sample_order.*` | 查看、基础维护、延期、特殊状态、延期性质、删除和平均考核分分别授权；主页角标按当前用户可处理事项计算。 |
| 异常单跟进 | 功能权限与通知已迁移 | `error.*`、`notifications.error.*` | 整单权限使用稳定编码；具体措施仍由记录中的负责人本人处理；延期和关闭审批目前使用稳定审批权限，尚未接入通用流程事件。 |
| 样品问题跟进 | 功能权限与通知已迁移；关闭流程已接入 | `sample_issue.*`、`notifications.sample_issue.*`、`sample_issue:close_request` | 关闭审批统一使用 `sample_issue.close.approve` 作为候选资格，默认组和研发电子组由流程条件及具体待办区分；数据库模式的新关闭申请必须命中已发布流程，不再使用旧角色路由回退。 |
| 设计知识库 | 已迁移 | `design_knowledge.*`、`design_knowledge:knowledge_review`、`design_knowledge:tag_review` | 知识发布和新标签申请均按已发布流程分配具体审批人；仅拥有审批权限但没有本单待办者不能代审。 |
| 统计信息 | 已迁移 | `statistics.view`、`statistics.overview.view`、`statistics.overview_owner.manage` | 页面入口、通用项目汇总、全员概述待办/负责人趋势和跨项目概述负责人配置分层授权；页面直达与保存回调均复核。 |
| ECN工程变更 | 已迁移 | `ecn.*`、`ecn:ecr_review`、`ecn:scheme_review` | 入口、申请、影响、方案、审批、执行、附件和删除分别授权；两段审批按已发布流程固化具体人员，方案流程支持节点内全部会签；执行责任不再匹配当前岗位名称。 |
| 固定通知接收 | 已迁移 | `notifications.*` | 固定收件人由“在职用户拥有通知权限 + 已绑定企业微信账号”解析；申请人、负责人等动态人员继续按单据字段解析。 |

### 6.2 身份、权限与流程公共 Python 文件职责

| Python 文件 | 作用 | 不应承担的职责 |
| --- | --- | --- |
| `src/identity_store.py` | 身份体系的 SQLite 数据访问层；建立和维护用户、密码哈希、部门、岗位、任职、附加权限组、权限关系、企业微信绑定、审批流程版本、具体待办和审计日志；执行 Excel 用户迁移。 | 不负责渲染 NiceGUI 页面，也不包含具体业务模块的按钮显示逻辑。 |
| `src/user_service.py` | 身份服务兼容门面；自动判断当前是旧 Excel 模式还是身份数据库模式，并向登录、管理页和业务模块提供统一方法；负责同步权限目录。 | 业务模块不应绕过它自行判断数据源，也不应直接读取身份表拼装权限。 |
| `src/legacy_compatibility.py` | 统一记录旧 Excel 身份、旧角色授权、旧通知/审批路由和历史审批快照的实际命中；按类别、功能和用户限频汇总，并输出可检索的 `LEGACY_COMPAT_HIT` 标记。 | 不改变权限结果、不代替兼容分支，也不因观察到零命中自动删除配置。 |
| `src/permission_catalog.py` | 全系统稳定权限编码的唯一目录；保存权限名称、所属界面分组、说明、废弃权限替换规则和旧工具权限的一次性初始化映射，并从 `overview_config.json` 动态生成和校验项目概述 `label` 权限。 | 不负责判断某个具体用户是否有权限，也不实现业务状态机。 |
| `src/access_control.py` | 已迁移模块统一的权限判断入口；数据库模式只调用稳定权限，旧 Excel 模式才使用调用方传入的兼容角色范围。 | 不保存授权关系，不解析审批人，也不应新增业务专属角色关键词。 |
| `src/project_access.py` | 集中封装项目基础资料维护、项目状态修改和全部项目工程师指定三项独立权限，并保持旧 Excel 模式角色兼容。 | 不保存项目资料，不处理项目需求审批，也不修改项目概述内容。 |
| `src/project_requirement_access.py` | 集中封装需求正文查看、维护、全局审批、本人负责项目审批、撤销和跨用户草稿权限；把稳定资格与项目工程师具体分配组合判断。 | 不保存需求文件，不修改审批状态，也不承担项目概述的专业角色权限。 |
| `src/project_todo_access.py` | 集中封装项目待办工作台入口，并按概述 `label` 维护权限过滤本人真正可处理的概述待办。 | 不授予需求或概述业务权限，不生成待办，也不执行审批和数据修改。 |
| `src/question_tree_access.py` | 集中封装需求项结构主页面与完整打印清单的统一查看权限，并保留旧 Excel 模式角色关键词兼容。 | 不加载或修改需求节点配置，也不参与项目需求审批。 |
| `src/project_test_summary_access.py` | 集中封装生产测试项汇总表的统一查看权限，并保留旧 Excel 模式登录用户兼容。 | 不聚合测试项、不控制概述原始数据维护，也不执行打印。 |
| `src/project_overview_access.py` | 集中封装概述 `label` 级查看/维护、失活查阅、原始内容管理、批量申请及两类审批权限，并解析概述审批流程和具体审批人快照。 | 不保存概述数据，不执行批量变更或纠错状态机。 |
| `src/statistics_access.py` | 集中封装统计页面入口、全员概述待办/负责人统计及跨项目概述负责人维护权限，并保留旧 Excel 模式的入口关键词和精确角色兼容。 | 不计算统计指标，不读取历史 Excel，也不修改概述负责人数据。 |
| `src/overview_permission_mapping.py` | 读取并校验旧概述 JSON 权限规则，统计旧角色覆盖范围，并把“一个旧角色到多个系统岗位”的映射换算成完整的岗位 `label` 权限计划。 | 不直接写数据库，不渲染管理页面，也不改变概述业务数据。 |
| `src/identity_codes.py` | 部门、岗位、附加权限组和流程等稳定编码的规范化、格式提示与后端校验。 | 编码只用于稳定引用和查重，不直接授予权限。 |
| `src/identity_matching.py` | 系统用户与企业微信通讯录成员的保守自动匹配；处理 userid、唯一同名、重名和账号争用，生成管理员可检查的匹配计划。 | 不直接调用企业微信接口，也不会在歧义情况下强制绑定。 |
| `src/identity_config_transfer.py` | 身份配置跨环境迁移；以稳定编码和用户名导出部门、岗位、权限、用户关联及流程，完成校验值检查、目标环境预检、导入前 SQLite 备份和单事务合并。 | 不导出或修改密码，不搬运业务记录、待办和历史审计，也不能代替根目录业务 JSON 的代码部署。 |
| `src/approval_workflow.py` | 通用审批业务事件目录、流程条件匹配、审批候选人解析、稳定权限复核、具体待办固化、串行节点推进与旧路由草稿导入。当前注册样品问题、设计知识、项目概述及 ECN 共七个业务事件。 | 多节点核心已支持串行及节点内会签，但尚未支持退回到指定节点、条件分支或加签；业务模块必须显式接入多节点接口，不能只修改流程配置。 |
| `src/ecn_access.py` | 集中封装 ECN 入口、申请、影响、方案、执行、附件、删除及主页待办权限；影响评估、方案编写与评审发起、初始提醒和分类附件查看只按身份数据库中的稳定权限判断，旧 Excel 岗位配置不再授权这些动作。 | 不修改 ECN 数据，不推进审批状态，也不定义执行路线拓扑。 |
| `src/ecn_workflow.py` | 适配 ECN 的 ECR 与方案评审业务事件，负责创建多节点具体待办、审批推进、驳回终止、流程快照读取，以及系统启动时按单据快照校准节点待办并清理无业务单据的 ECN 孤立待办。 | 不渲染页面，不校验方案内容完整性，也不执行 ECN 系统内资料落盘。 |
| `src/notification_recipients.py` | 把通知权限或流程配置的稳定岗位转换为当前在职且已绑定的企业微信 userid；数据库模式默认排除仅因系统管理员身份拥有全权限的 `admin`。 | 不生成通知正文，不决定申请人或负责人等动态收件人。 |
| `src/workflow_notifications.py` | 读取随单固化的流程完成抄送设置，合并重复 userid，并发送统一完成卡片或无链接文字消息。 | 不替换模块原有通知，不参与审批事务和审批权限判断。 |
| `src/wecom_service.py` | 企业微信底层集成；负责通讯录同步与缓存、成员解析、消息发送、失败重试和日志。 | 不负责系统内部岗位授权和审批流程匹配。 |
| `src/issue_workflow_utils.py` | 异常单和样品问题共用的日期解析、负责人判断、人员文本拆分、收件人合并及后台任务调度工具。 | 不保存模块状态，也不定义稳定权限目录。 |
| `src/db_storage.py` | 通用业务数据存储层；提供 `general_storage`、逐实体 JSON、缓存、事务锁和原子更新。身份表也位于同一 SQLite 文件，但身份关系由 `IdentityStore` 管理。 | 不作为权限判断入口；业务数据存在不代表当前用户有权读取或修改。 |
| `src/pages/manage.py` | “用户、组织与权限中心”的 NiceGUI 管理界面；组合用户迁移、企业微信绑定、部门岗位维护、岗位默认权限、附加权限组和审批流程草稿/发布/模拟。流程编辑器采用约 3:7 的策略导航与编辑区布局，以横向阶段概览和纵向节点连线表达审批顺序，字段使用紧凑分栏。 | 页面校验不能替代 `IdentityStore` 的事务校验；不应在这里硬编码某个业务角色名称。 |
| `src/pages/main_dashboard.py` | 系统主页模块卡片、稳定权限入口过滤和各模块红色待办角标汇总；`src/dashboard_layout.py` 按可见数量及视窗上限（1/2/4/5）优先等量分行，不能整除为多列时各行最多差一张，逐行居中。 | 只负责入口与摘要，不应成为业务模块唯一的权限防线；目标页面及保存回调仍须复核。 |
| `src/utils.py` | 提供 `sync_current_user_role()` 等会话公共工具；身份数据库模式下同步当前用户的岗位显示信息，兼容旧页面仍读取 `current_role` 的阶段。 | `current_role` 目前主要用于未迁移模块和历史留痕，不能重新用于已迁移模块授权。 |
| `scripts/migrate_users_to_iam.py` | 服务器命令行一键迁移入口；从执行机器当前的 `data/users.xlsx` 迁移用户并同步权限目录，默认不覆盖已迁移密码。 | 普通重复部署不得随意使用 `--refresh-existing-passwords`。 |
| `scripts/convert_excel_to_json.py` | 独立的 Excel 转 JSON 命令行工具；部署前从项目根目录归入脚本目录，应用运行时不引用。 | 不用于用户身份或配置包迁移。 |

### 6.3 已迁移业务模块对应 Python 文件职责

| 模块 | 主要 Python 文件 | 文件职责及迁移边界 |
| --- | --- | --- |
| 分析工具 | `src/pages/tool.py` | 定义 `/tool` 页面、工具卡片元数据和入口过滤，调用 `can_use_tool()` 检查 `tools.<tool_key>.use`。`src/tools/*.py` 继续负责各工具自身计算和界面，不应再各自判断角色名称。 |
| 项目资料总表 | `src/pages/project_table.py` | 定义 `/project_table` 页面，负责项目基础资料列表、分类筛选、状态数据范围、项目新增/修改和需求/概述/测试报告跳转；基础资料与状态字段分别复核权限。链接目标页面有各自的迁移边界，不能仅因能看到项目总表就获得需求或概述编辑权限。 |
| 项目需求正文 | `src/pages/requirement.py`、`src/pages/information.py` | `requirement.py` 的需求模式负责正文显示、上传、新建、暂存、自动保存和提交；`information.py` 的需求评审队列负责提交人返修、具体项目工程师或全局审批人处理、记录移除、审批撤销及草稿列表。 |
| 项目待办 | `src/pages/information.py`、`src/pages/main_dashboard.py` | `information.py` 定义 `/information` 聚合工作台，组合本人概述待办、需求评审/草稿及概述变更申请；主页负责入口和角标摘要。两处统一复用 `project_todo_access.py`，内部业务操作仍由各业务访问层复核。 |
| 需求项结构 | `src/pages/question_tree_tabs.py` | 定义 `/question_tree_tabs` 层级结构浏览页与 `/print_list` 完整清单页，读取 `config_service.json` 的需求节点配置，提供路径追溯、搜索、打印；两个路由共用稳定查看权限，不写业务数据。 |
| 生产测试项汇总 | `src/pages/test_summary.py`、`src/pages/project_table.py`、`src/pages/requirement.py` | `test_summary.py` 定义项目测试项只读汇总与打印页面；项目资料表和项目概述页提供入口，并在显示和点击时复核同一权限。汇总数据来自项目概述中的启用测试项。 |
| 项目概述 | `src/pages/requirement.py`、`src/components.py`、`src/pages/information.py`、`src/overview_batch_operations.py`、`src/overview_corrections.py` | `requirement.py` 的概述模式负责页面、专业分组、项目状态/负责人和批量申请；两个组件负责普通项/表格项的读写与纠错申请；`information.py` 负责本人概述待办和两类审批；两个服务文件负责批量变更及纠错的校验和原子执行。 |
| 样品订单看板 | `src/pages/sample_order_dashboard.py` | 定义 `/sample_order_dashboard` 页面，负责逐订单存储迁移、导入、日期与考核计算、增删改、待办筛选、权限复核和企业微信通知；这是样品订单业务的主要实现文件。 |
| 样品订单配置 | `src/sample_order_dashboard_config.py` | 读取 `sample_order_dashboard_config.json` 中的预警天数、特殊状态及通知行为等业务配置；角色列表只供旧 Excel 模式兼容，不参与身份数据库模式授权。 |
| 异常单跟进 | `src/pages/error_management.py` | 定义 `/error_management` 页面，负责异常单数据模型、原子并发更新、负责人局部处理、延期/关闭申请审批、附件、提醒、待办角标、稳定权限复核和通知发送。 |
| 异常单配置 | `src/error_management_config.py` | 读取 `error_management_config.json` 中的产品状态、筛选状态、提醒周期、通知开关等业务配置；旧编辑/审批角色和旧通知目标仅用于 Excel 模式。 |
| 样品问题跟进 | `src/pages/sample_issue_collection.py` | 定义 `/sample_issue_collection` 页面，负责样品问题录入、对策、延期、关闭、试产前特殊准备、附件、并发更新、待办角标、稳定权限和企业微信通知；关闭申请在这里调用通用审批引擎并保存流程及审批人快照。 |
| 样品问题配置 | `src/sample_issue_config.py` | 读取 `sample_issue_collection_config.json` 中的业务状态、提醒、特殊准备和旧通知路由；关闭路由角色只供旧模式及生成流程草稿使用。 |
| 设计知识库 | `src/pages/design_knowledge.py` | 定义 `/design_knowledge` 页面，负责知识与标签数据、附件、筛选、状态流转、入口与操作权限、知识/标签审批具体待办以及主页角标；数据库模式的新审核必须命中管理员发布的流程。 |
| 设计知识配置 | `src/design_knowledge_config.py` | 读取 `design_knowledge_config.json` 中的内容类型、专业领域、等级、默认标签、表单文案和附件配置；角色路由只供旧 Excel 模式、旧待审核记录收尾及一次性流程草稿导入。 |
| 统计信息 | `src/pages/statistics.py` | 定义 `/statistics` 页面，负责项目状态、需求/概述完成度、全员待办和负责人趋势图表，并提供跨项目概述负责人配置；基础汇总与个人待办/负责人统计分层显示，负责人写入在打开和确认时双重复核。 |
| ECN工程变更 | `src/pages/ecn_management.py` | 定义 `/ecn_management` 页面及完整 ECN 状态机，负责 ECR 表单、影响评估、方案协作、两段审批、资料自动执行、物料追溯确认、附件和列表待办；页面调用 `ecn_access.py` 和 `ecn_workflow.py`，数据库模式不直接用岗位名称授权。 |
| ECN业务配置 | `src/ecn_management_config.py` | 读取 `ecn_management_config.json`，维护方案选项、追溯范围、状态映射和通知参数，并提供执行清单骨架及覆盖率等纯业务函数；审批和执行路线统一由数据库流程管理。 |

### 6.4 测试文件与主要覆盖范围

- `tests/test_access_control.py`：身份数据库与旧 Excel 模式切换、岗位/附加权限组授权、`admin` 自动权限、项目资料数据范围、项目待办入口与可处理 `label` 过滤、需求项结构及生产测试项汇总的统一查看权限、需求正文和项目概述的旧模式兼容、ECN 分项权限/附件分类/执行责任权限、稳定权限、`label` 级隔离、动态目录同步、重复检测、专业级旧授权展开及具体审批人隔离，以及已迁移模块停止继承 `legacy.*` 授权。
- `tests/test_overview_permission_mapping.py`：旧角色覆盖统计、一对多岗位映射、多个规则权限合并、映射持久化、只替换概述项权限，以及取消映射后的安全清理。
- `tests/test_identity_store.py`：用户、组织、岗位、任职、企业微信绑定、权限关系和迁移事务等底层行为。
- `tests/test_identity_matching.py`：企业微信自动匹配、重名、争用和歧义保护。
- `tests/test_identity_config_transfer.py`：配置包敏感字段隔离、稳定编码重建、服务器密码与独有用户保留、流程具体人员重映射、文件篡改拒绝、导入前备份和事务失败回滚。
- `tests/test_approval_workflow.py`：流程版本、优先级冲突、稳定岗位 ID、具体审批待办、多节点串行、节点内任意/全部会签、ECN ECR 多节点推进、中断后节点待办自愈与孤立待办清理、样品问题/设计知识/项目概述旧路由草稿导入和样品问题关闭集成。
- `tests/test_workflow_notifications.py`：完成抄送默认关闭、稳定岗位配置读取、原收件人与抄送账号去重及卡片发送。
- `tests/test_sample_order_dashboard.py`：样品订单权限、状态、日期、导入和待办计算。
- `tests/test_error_management_concurrency.py`、`tests/test_error_management_config.py`：异常单原子更新和配置解析。
- `tests/test_sample_issue_collection.py`：样品问题状态、责任人、延期/关闭和权限边界。
- `tests/test_design_knowledge_config.py`、`tests/test_design_knowledge_workflow.py`：设计知识旧模式兼容、稳定权限、两个审批事件和具体审批人隔离。
- `tests/test_statistics.py`：统计分类和负责人完成度聚合；统计入口及细权限的旧模式和数据库模式隔离由 `tests/test_access_control.py` 覆盖。分类断言必须与图表当前使用的“概述无负责人”“有待定”等数据键保持一致。
- `tests/test_utils_overview_config.py`：概述配置在后台启动时禁止创建 UI 通知，以及通知 slot 消失时不影响核心同步结果。

### 6.5 旧 JSON、角色常量和历史字段的当前处理原则

- 已迁移模块在身份数据库模式下不再从旧角色常量或根目录 JSON 获取授权；权限计算只合并主岗位默认权限、非兼容附加权限组和 `admin` 自动权限。
- `tools_permission.json` 当前只用于旧 Excel 模式和权限目录首次初始化，不能把它当作数据库模式的实时权限配置。
- `module_show_role.json` 中需求评审、草稿及概述待办/变更角色列表不再参与数据库模式授权；文件仍可能被其它尚未迁移页面读取，当前不能直接删除。
- `sample_order_dashboard_config.json`、`error_management_config.json`、`sample_issue_collection_config.json` 和 `design_knowledge_config.json` 仍包含有效业务参数，不能整文件删除。文件中的旧角色、旧职务通知目标或审批路由只属于部署兼容和流程草稿导入范围。
- 单据中保存的 `created_role`、`updated_role`、`requester_role`、`approver_roles` 等字段继续作为历史审计或迁移前审批快照；新代码不得仅凭这些显示文本授权。
- 新审批申请固化流程编码、版本、所需权限和具体审批用户名。后续岗位改名或人员调岗不会改变已创建申请的审批快照；人员离职后的待办转交仍应通过后续待办转交功能或明确业务操作完成，不能临时修改旧角色名称冒充转交。
- 在服务器完成身份迁移、剩余业务模块全部重构且历史兼容窗口结束前，不删除旧角色常量、配置加载器或相关 JSON。最终清理前必须再次执行全仓搜索，并逐项确认其只剩兼容引用。
- 清理兼容代码前应覆盖一个有代表性的业务周期检索 `logs/app.log*` 中的 `LEGACY_COMPAT_HIT`。`excel_user_store` 表示服务器仍在使用 Excel 用户源，`legacy_role_grant` 表示旧角色仍实际放行操作，`legacy_notification_route`/`legacy_workflow_route` 表示旧 JSON 路由仍在运行，`legacy_approval_snapshot` 表示迁移前待审记录仍在按历史快照收尾；任何一类仍有命中都应先分析对应 `feature`，不能整批删除。

### 6.6 尚未完成迁移的主要范围

- ECN 已完成稳定权限、两段可配置审批、七类执行流程、具体人员快照、附件分类权限、执行责任权限、审批节点待办自愈和全库孤立待办扫描。新单据必须使用数据库身份和已发布流程；ECN 的 JSON 审批/执行路线及影响、方案、提醒、分类附件权限配置和对应旧模式兼容分支已删除。
- 异常单延期/关闭、样品问题延期等已经使用稳定审批资格但尚未接入通用流程引擎的事项，属于后续流程配置能力完善，不等同于整个模块尚未迁移。
- 其它未列入 6.1 的模块应视为“尚未确认完成”，不能因为公共权限框架已经存在就默认它们已迁移。

## 7. 维护约定

后续处理项目任务时，优先在本文追加以下信息：

- 新读懂的模块职责、入口、关键数据键及外部依赖。
- 已确认的业务规则和角色权限，不记录未经验证的猜测为事实。
- 重要设计决策及原因。
- 已复现的缺陷、修复位置、覆盖测试和仍存在的风险。
- 配置、数据库结构、启动方式或部署方式发生变化时，同步更新对应章节和“最近更新”日期。
- 新增或修改上传交互时，优先使用 `src/custom_ui.py` 的 `custom_upload()`，保持文件移除、清空、大小汇总和样式行为一致；只有在自定义控件明确无法满足需求时才直接使用 NiceGUI 原生 `ui.upload`，并记录原因。
- 后续新增或修改的代码注释和文档字符串统一使用中文；第三方接口名、协议名、参数名及无法准确翻译的技术标识可以保留英文。

静态类型质量约束：

- 新增或修改 Python 功能在交付前必须同时执行语法编译、相关专项测试和编辑器静态类型检查；不能只以运行成功作为完成标准。
- 静态类型检查必须覆盖完整受影响引用链，并定期覆盖整个 `src/` 与 `tests/`；不能只检查直接改动文件。尤其要检查测试夹具中的异构字典推断、可空返回值收窄，以及框架返回的 `object` 在调用属性前是否完成 `isinstance` 判断。
- VS Code Pylance 扩展中的 `dist/pyright.bundle.js` 是语言服务器构件，不可当作 Pyright CLI。命令无输出且退出码为 0 不代表类型检查通过；必须以编辑器 Problems 的工作区诊断或真正可执行的 Pyright CLI 结果为准，并实际确认诊断数量为零。
- 不用宽泛的联合类型字典向参数类型更严格的数据类或函数做 `**kwargs` 展开；统计结果等固定结构应优先使用数据类或 `TypedDict`，让每个字段的可空性准确表达。
- `T | None` 值在访问属性或传给要求 `T` 的参数前必须显式判空或收窄。由列表筛选、外部框架装饰器等产生的隐含条件，不假定静态分析器能自动推断。
- 相关变量之间存在业务上的联动关系时，也必须分别完成类型收窄。例如“流程结果非空意味着审批快照已生成”不能只依赖前一段赋值，应在读取快照前显式判空并返回可理解的业务错误；从异构 JSON 字典取得的状态值用作 `dict[str, ...]` 的键之前，应先规范化为 `str`。
- NiceGUI 的 `app.storage.user.get()` 在 Pylance 中可能被推断为 `Unknown | None`；登录页校验不能只写成“先判断一次、再重新调用 `get()`”。应把结果保存到局部变量，通过 `isinstance(value, str)` 和非空判断完成收窄，再将该局部变量传给要求 `str` 用户名的权限函数，避免再次出现 `reportArgumentType`。
- 异构 JSON 根字典不能只靠第一个字面量推断类型。例如先以 `{"0": {"file_dic": {}}}` 初始化、后续又写入字符串或布尔值时，应显式标注为 `dict[str, object]`（结构固定时优先 `TypedDict`），并在调用嵌套字典方法前用 `isinstance` 收窄。避免再次出现 Pylance 将整个字典误推断为 `dict[str, dict[...]]` 后拒绝写入 `str` / `bool` 的问题。
- 从 `dict[str, object]` 或外部JSON读取的值不能直接传给 `int()`、`float()` 等只接受可转换类型的构造函数；应先用 `isinstance` 收窄到受支持的标量类型，再捕获格式及溢出异常。ECN追溯负责人节点的 `stage_index` 统一通过 `get_ecn_stage_index()` 解析，无效值回退为首节点，避免再次产生 Pylance 的 `object`/`ConvertibleToInt` 类型错误。
- NiceGUI 类实例中的可刷新方法必须使用框架专用的 `@ui.refreshable_method`，首次渲染用 `self.method()`，后续用 `self.method.refresh()`。不能用 `@ui.refreshable` 配合 `ClassName.method(self)` 绕过类型提示：这种写法会把刷新目标登记到空实例，导致后续文件列表、结果区等实例 UI 无法刷新。
- 测试代码同样必须通过静态检查。`TemporaryDirectory.name`、迁移结果中的可空路径等值，在传给 `Path()` 前先用断言收窄；`AsyncMock.await_args` 的类型是可空调用记录，即使前面已经断言调用次数，也要先保存到局部变量并断言非空，再读取 `.args` 或 `.kwargs`。
- 不使用 `type: ignore`、关闭诊断或把类型扩大成 `Any` 来掩盖本可建模的问题；确需例外时必须记录框架限制和运行验证依据。

不要在本文记录密码、密钥、个人信息、真实业务单内容或其他敏感数据。
