# 项目上下文与维护记录

> 用途：为后续开发、排查和代码审查保存项目级背景。本文是持续更新的“活文档”，不是用户操作手册。
>
> 最近更新：2026-08-18；补充全工作区静态类型检查要求，并明确 Pylance 语言服务器构件不能代替 Pyright CLI。

## 1. 项目概览

- 项目名称（由程序标题推断）：研发项目文件管理系统。
- 技术形态：基于 NiceGUI 的 Python Web 应用，页面、交互和后端业务逻辑主要都在 Python 中实现。
- 入口：`src/main.py`。根路由跳转登录页，应用默认监听 `0.0.0.0:8080`。
- 页面注册：`src/main.py` 导入 `src/pages`，`src/pages/__init__.py` 再导入所有页面模块，从而执行各页面的 `@ui.page` 路由注册。
- 主要业务范围：项目资料/概述、需求、ECN 工程变更、异常管理、样品问题与样品单、统计、设计知识及工程工具。
- 登录态：页面通常从 `app.storage.user` 读取 `current_user`、`current_role`；ECN 未登录时跳转 `/login`。
- 本地命令行启动默认保留 NiceGUI 热重载；VS Code 的 NiceGUI 调试配置注入 `NICEGUI_RELOAD=false`，避免 Windows + Python 3.13 下 watchfiles 重启进程时触发 debugpy/pydevd 线程补丁异常。需要热重载时使用普通命令行启动，或显式设置 `NICEGUI_RELOAD=true`。

## 2. 数据与运行时架构

### 2.1 持久化

- 统一存储模块：`src/db_storage.py`。
- SQLite 文件：`db/nicegui_storage.db`。
- `general_storage`：顶层 key 对应一段 JSON，兼容 `get_item`、`set_item`、深层路径读写和原子深层更新。
- `json_entity_storage`：按 `namespace + entity_id` 拆分的实体存储，供已迁移的业务模块使用。
- 读取优先走进程内缓存，默认返回深拷贝；写入使用异步 SQLite、进程内资源锁及数据库事务锁。
- 应用启动时 `master_startup()` 先执行 `db_storage.init_db()`，然后加载概述配置和各后台任务；关闭时关闭数据库。

### 2.2 NiceGUI 全局数据

项目资料相关的运行时索引主要放在 `app.storage.general`，ECN 直接依赖以下键：

- `project_summary`：项目名称及项目状态，用于生成可选项目树。
- `project_req_max_ver`：每个项目当前最高需求版本，最终执行 ECN 时用于生成新概述节点的版本激活信息。
- `over_config_data` / `over_config_data_flat`：概述技术维度、分组、字段配置和处理类型。
- `requirement_overview_impact_config`：从根目录 `requirement_overview_impact.json` 校验加载的
  `node_id -> overview label` 影响映射；审批过程只读取该内存配置。
- `user_preferences`：头像等用户偏好。

管理员页“配置需求对概述影响”工具以 `app.state.init_config_data` 提供需求节点及说明，
以 `app.storage.general["over_config_data_flat"]` 提供合法概述项，并优先从当前审批内存载入草稿。
保存时先校验全部映射，再原子替换 `requirement_overview_impact.json` 并同步
`requirement_overview_impact_config`；内存写入失败会恢复旧文件和旧内存，因此保存成功后无需重启服务。
落盘及同步到内存的 `node_impacts` 均按 `node_id` 数值升序排列，避免按字符串顺序把 `10` 排在 `2` 前面。
显式配置空数组表示该节点不影响任何概述，删除节点映射则按 `unmapped_policy` 执行全概述兜底或阻止审批。

需求审批不会先覆盖正式概述文件：系统按目标版本生成唯一候选文件，从该版本的
`added` / `deleted` / `modified` 提取 `node_id`，原子更新概述状态后才发布候选文件并标记已审。
发布失败时会恢复文件快照和审批状态，并以原子更新内采集的概述前后快照进行比较回滚。
需求版本差异按有效业务答案比较：单选/下拉单选只比较当前选中值，多选只比较值为 `True`
的已选项集合。模板增删未选选项而产生的缺失键或 `False` 键不属于需求变更，不应生成
“更改为”记录或触发关联概述待定；实际选中值、已选集合、公差或引用文件变化仍属于变更。

静态文件目录主要为 `img/`、`uploads/`，文件 URL 前缀为 `/files`。

## 3. ECN 管理模块

### 3.1 模块定位

- 文件：`src/pages/ecn_management.py`。
- 路由：`/ecn_management`。
- 业务含义：先提交 ECR（工程变更请求），经过可行性审批后协同编制 ECN 影响与实施方案，再经过方案评审、各部门执行确认，最终将可自动执行的变更写入项目概述数据并关闭单据。
- 页面结构：主页面是可搜索、按状态筛选的 ECN 列表；详情使用最大化对话框，包含“ECR-申请、ECN-影响、ECN-方案、ECN-执行、审批记录”五个页签。

### 3.2 ECN 数据模型

`get_ecn_template()` 是新建ECN时使用的唯一标准结构；现阶段不保留旧单据字段迁移或模板补齐逻辑。主要字段如下：

- `ecn_id`、`form_no`。
- `basic_info`：标题、申请部门/人/日期、需求日期、文件号、永久/临时性质、ERP 单号、变更原因、变更要求及原因说明。
- `target_projects`：ECR 初始变更项目。
- `review_info`：扩大影响项目、影响维度、必出方案的资料项、物料类别与动作矩阵。
- `execution_info`：追溯等级、报废/返工措施、试产结论。
- `change_items`：具体变更方案。
- `workflow`：当前状态、阶段、步骤、路由类型、待处理角色、当前步骤审批结果和方案参与人状态。
- `approval_log`：审批及操作时间线。
- `timestamp`：单据内的业务时间记录；跨客户端列表刷新另用全局版本戳。

数据结构策略：功能开发阶段数据库基本无正式数据，ECN记录只读取当前结构。调整字段时直接同步修改模板、写入、读取、展示和测试，不增加旧字段回退分支。

### 3.3 编号与项目范围

- 编号格式：`ECN + YYMMDD + 两位日序号`，例如 `ECN26080701`。
- 新建时扫描当天已有单号，取最大两位尾号加一。
- 申请部门根据当前角色包含的关键词映射：研发、销售、工程、生产、质量、采购、PMC；未匹配时为“其它部门”。
- ECR 初始变更对象只能从状态为“试产”或“量产”的项目中选择。
- 进入方案阶段后，可把影响范围扩大到其他试产/量产项目，也可扩大到非试产/量产项目。

### 3.4 状态机与审批路由

主干流程：

```text
草稿 / 已被驳回
  -> ECR 审批中
  -> ECN 方案编写与确认中
  -> ECN 方案评审中
  -> ECN 等待各部执行确认
  -> 等待最终数据变更
  -> 变更已完成
```

旁路状态：ECR 审批中可由申请人撤回至草稿或作废；任一 ECR 审批驳回进入“已被驳回”；方案评审或执行阶段驳回均回到方案编写阶段，并把已有参与者重置为 `editing`。

具体路由来自根目录 `ecn_management_config.json`，由 `src/ecn_management_config.py` 校验加载；
`src/config.py` 仅保留原常量的兼容导出：

- 销售发起 ECR：销售总监 -> 研发经理。
- 非销售发起 ECR：研发经理 -> 销售总监。
- ECN 方案评审：研发经理 -> 销售总监 -> 工程、质量、PMC 并行会签。
- 执行确认：工程、生产、PMC、质量并行会签 -> 研发经理最终执行。

当前步骤用 `pending_roles` 表示；并行节点中所有角色的 `step_approvals` 都为真才进入下一步。审批日志记录用户、角色、动作、时间及可选意见。

### 3.5 权限逻辑

- ECR：仅申请人可编辑自己的草稿/驳回单并重新发起；申请人在 ECR 审批中可撤回或作废。
- 方案编写：当前角色名称包含研发、工程或质量时可编辑影响项和方案。
- 方案发评审：研发经理或 `admin`；至少有一名方案参与者，且所有参与者都已确认完成。
- 审批/会签：当前角色必须出现在 `pending_roles`。
- 最终执行：状态为“等待最终数据变更”且当前角色包含“研发经理”。
- 永久删除：列表仅向用户名为 `admin` 的用户显示删除入口。

注意：权限主要由页面按钮显示和回调入口控制，状态迁移函数本身没有再次完整校验操作者权限；后续若增加 API 或其他调用入口，应补服务端授权校验。

### 3.6 影响与方案完整性

- “相关影响”是告知型维度。
- 勾选的资料类型和“物料类别-动作”组合都是强制交付项。
- 资料方案分成两个并列大类，并各自使用独立新增入口及折叠栏：
  - 普通资料变更方案：`text_desc` / `scheme_category=ordinary_document`，不直接绑定概述数据。
  - 概述资料变更方案：`overview_update` / `scheme_category=overview_document`，与项目概述字段绑定，可选择更新已有节点或添加节点，支持多项目统一方案及文件类字段。
- ECN 方案折叠栏固定按“系统内资料变更方案、普通资料变更方案、物料变更方案”排列；没有条目的分类不显示。异常分类只进入“未识别方案”防护组，不进行字段推断。
- 普通资料、物料变更和系统内资料方案使用同一套可选项目范围：ECR申请时填写的 `target_projects`，加影响评审返回的 `expanded_projects_mass` 与 `expanded_projects_non_mass`，按该顺序去重展示；三类方案都必须选择至少一个目标项目，并统一保存到方案条目的 `projects` 字段。
- 方案展示采用“资料分类折叠栏 + 舒适型对比表格”，不再按项目增加第二层折叠。每个方案固定一行，列为编号、变更对象/类别、项目、动作、当前内容、执行后结果、追溯、处置、变更要求、影响勾选、处理人、状态和操作；变更要求独立显示 `req_idxs`，影响勾选列分开列出关联资料和物料动作，无关联时明确显示“未关联”。系统内资料的多项目统一方案在项目、动作、当前内容、执行后结果四列使用等高子行横向一一对应，避免靠上下顺序猜测归属。原内容保留原样、不使用删除线，处理人直接显示完整姓名。有驳回历史的方案在操作列显示历史按钮，点击后通过独立弹窗按最新在前查看全部驳回意见、审核人和时间，不再在数据行下方展开。表格常态使用中性色，仅待改进/待重新确认/完成使用语义色，窄视口通过表格区域横向滚动保持各列对齐。
- ECN 方案页签内容最大宽度为 `1900px`，在常见 1920px 宽屏下尽量利用弹窗可用空间；视口不足时仍由方案表格区域横向滚动，不影响其它页签的宽度布局。
- 非基准列选择“作为新数据添加”并绑定基准行后，系统按 `row_id + 当前需求版本` 检查该具体参数的同一格是否已有激活数据；若已有，在选择框右侧显示不占额外行高的红色警告按钮。用户首次选中该冲突组合时，浮层自动在按钮下方弹出并按 JSON 配置的秒数关闭；后续可点击按钮再次查看当前内容及本单已暂存的同格新增内容。该提示只警示“一格多数据”，不禁止确有需要的继续新增。
- 系统内资料方案把新增位置当时的数据库内容和本单其它暂存新增固化到 `project_states.<项目>.existing_contents`。表格使用“新增 / 更换 / 失效”动作，并为每个项目明确展示“当前内容”和“执行后结果”：新增且数据库当前已有内容时只用淡色文字显示“存在现有内容”，悬停后再查看具体内容，避免展示大概率与本次新增无关的原文；更换说明原内容失效并显示更换目标，失效固定说明不生成新内容。第一列新增统计该参数当前全部激活内容，非第一列新增只统计所绑定基准行的同格内容。
- 系统内资料方案的 `file`、`image` 类型在ECN方案表格中复用 `components.FileThumbnail`：更换/失效的当前内容显示可操作的旧文件或旧图缩略图，新增/更换的执行后结果显示新文件或新图缩略图；图片点击查看可缩放大图，PDF点击打开，其它文件点击下载。文件缺失时只显示失效提示。该能力只接入ECN展示层，不修改 `components.py` 内既有概述折叠栏逻辑。
- `search`、`svn` 等路径校验型概述方案不能只依赖布尔状态：校验成功时会保存由“处理类型、文件名、目标项目顺序、技术维度、具体参数”组成的校验签名。输入或上下文变化会立即失效，确认保存时再次严格比对；校验异步执行期间发生变化也不会接受旧校验结果。编辑弹窗打开时根据已有具体参数主动从 `over_config_data_flat` 初始化完整配置和处理类型，不依赖用户切换选择框触发；同时使用方案数据的深拷贝草稿，校验时直接读取当前输入控件值，收集本单暂存依赖覆盖时排除正在编辑的方案，避免一次失败输入污染原方案或反向覆盖自己的校验依赖。
- 系统内资料方案的文本、检测类型与项目概述录入保持一致：存在新增或更换项目且新内容为空时，自动带入该参数配置的 `dialog_placeholder` 作为格式示例；已有方案内容不会被覆盖。文件、图片、视频及路径校验类型仍使用各自的上传或文件名提示；全部项目均选择失效时不要求新内容或路径有效性校验。
- 物料方案：`text_desc` / `scheme_category=material`，方案分类由 `scheme_options.material_change_types` 的稳定语义键配置（`add / adjust_quantity / discontinue / replace`），当前文案为“新增、调量、弃用、更换”，明细只以结构化 `material_change` 保存。新增/弃用记录物料名称、用量、单位；调量记录物料名称、改前/改后用量、单位；更换分别记录改前和改后的物料名称、用量、单位。默认单位由 `scheme_options.material_default_unit` 配置，当前为 `pcs`。方案表格沿用“变更前 → 变更后”对照：新增显示“无 → 新物料”，弃用显示“旧物料 → 弃用”，调量及更换并列显示前后值。
- 每条物料方案必须选择至少一个追溯处置范围，只保存为 `traceability_levels: list`。`disposition_measure: str` 严格表示旧料处置措施，仅 JSON 配置 `scheme_options.material_disposition_required_types` 指定的稳定语义键对应分类显示并必填，当前为 `discontinue / replace`（弃用、更换）；`add / adjust_quantity`（新增、调量）及所有非物料方案不显示、不保存该字段。选择 `scheme_options.disposition_condition_required_measures` 中的处置方式（当前为“有条件用完止”）时，必须填写 `disposition_condition` 具体条件。非物料方案的追溯处置范围为选填。可选值集中在 `ecn_management_config.json` 的 `scheme_tracking` 和 `scheme_options` 中维护。必填字段缺失时完整性看板会提示并阻止发起评审，相关内容也进入驳回前后方案快照。
- 追溯等级按 JSON 数组顺序表示由浅到深；仅在用户新勾选某一级的当次操作中，自动扩选该项上方全部等级。后续手动取消任意中间等级时保留断层，不自动补回；打开、保存和展示阶段也不做连续化规范。选择框固定为单行摘要“已选择 N 项”，不使用会换行撑高弹窗的已选标签；展开列表仍显示全部等级。方案表格将“追溯等级”和“处置措施”作为独立列：选填方案已配置时显示具体值，未配置时显示“—”；物料必填项缺失时显示“未配置”。
- 方案表格“旧料处置措施”主文字与“执行后结果”使用相同字号；报废、返工、有条件用完止依次使用红、橙、琥珀警示色，其它处置措施保持中性灰色，具体条件作为次级文字显示。
- 每个方案都可记录关联的目标项目、ECR 要求序号，并按类别关联已勾选的资料或物料项。添加/编辑弹窗通过直接展开的复选框组选择这些关联项，避免下拉多选中内容截断和反复开合；系统内资料方案勾选项目后实时重建多项目配置矩阵。底层仍保存为原有列表字段。界面将关联区标为必填，但当前单条方案保存回调并未强制校验要求/影响关联列表非空。
- 覆盖率看板同时检查三类强制关联：每一条 ECR 变更要求、已勾选资料、已勾选物料动作。任一变更要求没有至少一个方案通过 `req_idxs` 关联，或资料/物料有缺项，系统都会显示具体遗漏项并阻止发起 ECN 方案评审；物料追溯/处置缺失提示使用当前方案表格的全局编号（如“方案 #03”），不显示内部 UUID。“方案已齐”状态和入口提醒复用同一覆盖口径。
- ECN 模块的可维护业务选项统一从 `ecn_management_config.json` 读取：`scheme_options` 管理系统内资料动作文案、普通资料/物料方案分类、默认物料单位、需旧料处置的物料分类及需填写条件的处置方式；`scheme_tracking` 管理追溯范围和旧料处置选项；`schema` 管理 ECR 原因/影响/资料/物料及执行阶段处理措施、试产结论。代码中保留流程字段结构、稳定语义键、布局和状态机逻辑，不把 CSS、控件行为或内部状态编码配置化。
- ECN 影响区对扩大影响项目的添加/删除，以及“相关影响”选项的勾选/取消进行字段级审计，追加保存在 `review_info.impact_change_log`。每条记录包含唯一 `event_id`、`user`、`role`、`time`、`field`、`target`、`action`、`before`、`after`；原子保存按 `event_id` 追加去重，不用某个用户的本地副本覆盖已有日志。`workflow.impact_handlers` 仍只表示参与维护影响区的人员名单，不能代替字段级审计。
- 添加或修改方案会把当前用户加入 `scheme_participants` 并设为 `editing`；用户可确认完成或重新开启编辑。
- 处理人确认完成方案后，参与者状态为 `confirmed`，不再显示主页入口角标或列表“待处理”；若审批人驳回其方案，才重新进入提醒状态。
- ECN 方案审批驳回必须选择一个或多个具体方案。所选方案标记为 `needs_improvement`（待改进）并记录当前驳回信息及历史；只有所选方案的作者转为 `needs_reconfirmation`（待重新确认），未被选中的方案及其他已确认处理人保持原状态。
- 作者修改被驳回方案后，方案状态转为 `revised_pending_confirmation`；存在尚未修改的被驳回方案时不允许重新确认。作者重新确认后，已整改方案转为 `revised_confirmed`，参与者恢复 `confirmed` 并停止提醒。
- 每次定向驳回只写入 `rejection_history`，并在当前记录保存排除审计/运行字段后的完整 `before_snapshot`；作者保存整改方案时，在同一记录补充 `after_snapshot`。快照覆盖项目、变更对象、要求/影响关联及变更前后内容，历史弹窗可逐次对照查看。

### 3.7 最终执行逻辑

只有 `overview_update` 会自动修改各项目的 `<项目名>_over_data`：

- 更换：旧节点在当前需求版本失活并被标为禁用；创建新节点，继承旧节点的 `row_id`，写入新内容和 ECN 依据。
- 新增：创建新的概述 Chip；若是表格基准列则先生成 `row_id`，其他列再通过临时锚点绑定该行。
- 失效：只让选中的原概述节点失活，不创建对应新节点；多项目方案允许与新增、更换混合选择。
- 执行前先排序，保证同一表格的基准列早于后续列落盘。
- 成功处理的自动方案标记 `execute_status=success`。
- `text_desc` 方案是人工执行记录，不会自动写入项目概述。
- 最终将 ECN 状态置为“变更已完成”并清空待处理角色。

### 3.8 并发与刷新

- ECN 集合存储键：`ecn_management_data`，结构为 `{ecn_id: ecn_data}`。
- 所有 ECN 专属写入代理在成功后更新 `ecn_global_version_stamp`。
- 主列表每 5 秒只比较版本戳；变化后才重新读取和渲染列表，避免每轮遍历所有 ECN。
- 方案编写详情每 3 秒同步当前单据的工作流、方案参与者、方案条目和影响信息。
- 方案、参与者状态和影响信息采用深层原子更新，降低多人编辑互相覆盖的概率。

### 3.9 ECN 角标提醒规则

- ECN 提醒、权限、参与人/单条方案状态显示及驳回整改状态映射集中在根目录 `ecn_management_config.json`，修改后需要重启服务。
- 常规审批节点仍提醒 `pending_roles` 中的角色；草稿或 ECR 驳回仍提醒申请人。
- ECR 审核通过进入方案编写后，若 ECN 影响区为空且没有具体处理人，只提醒配置的初始角色（当前为“研发助理”）。
- 第一个用户在 ECN 影响区产生有效选择后，用户名写入 `workflow.impact_handlers`；其他用户后续参与处理时继续追加，支持多人负责。
- 一旦已有具体影响处理人，不再向所有研发/工程/质量角色泛化提醒，也不再额外提醒未参与的研发助理。
- 具体处理人在方案编写阶段仅当参与状态配置为需要提醒时显示角标：默认 `editing` 和 `needs_reconfirmation` 提醒，`confirmed` 不提醒。方案被定向驳回后，仅提醒被驳回方案的作者，直至完成修改并重新确认。
- 当方案参与者全部确认，且勾选的资料、物料动作均被方案完整覆盖时，单据进入“方案已齐，待发起评审”的业务条件；此时 `scheme_initiator_roles`（当前为研发经理、admin）也会收到主页红色角标，直至有人发起方案评审。
- 升级前没有 `impact_handlers` 的旧单：影响为空时提醒研发助理；影响非空时优先用已有 `scheme_participants` 作为处理人兜底，仍无法识别时提醒研发助理，避免无人负责。
- 主页红色角标与 ECN 列表内“待处理”标签共用 `is_ecn_pending_for_user()`，保持统计口径一致。

## 4. 当前已知风险与后续关注点

以下是静态阅读得到的工程风险，修改前应通过复现或测试确认：

1. `ecn_management.py` 接近 19 万字符，UI、状态机、权限、数据迁移和最终执行高度耦合，修改一处容易产生跨阶段回归。
2. 最终执行会逐条写项目概述，之后再单独原子更新 ECN 状态；它不是覆盖所有项目写入的单一事务，中途异常可能留下部分项目已变更、ECN 未关闭的状态。当前也没有显式幂等执行保护。
3. 新建 ECR 的编号由“先读全部数据再取最大序号”生成，并发新建可能撞号；两位序号在单日超过 99 后也不再满足固定宽度假设。
4. 状态迁移逻辑在 `execute_db_action()` 的本地预处理和 `state_machine_transition()` 中存在两份相似实现，后续容易不一致；新单首次提交的日志是否会重复尤其值得加测试验证。
5. ECN 仍以 `general_storage` 中一个大 JSON 集合保存。深层原子更新虽然能防覆盖，但单条单据更新仍可能读写整个顶层 JSON；数据量增大后可评估迁移到实体存储。
6. 角色判断混用了精确匹配和子串匹配，例如 `current_role in pending_roles` 与“角色名包含研发”；角色命名变化可能导致按钮或审批权限异常。
7. 方案详情同步通过 `str(list)` 比较变化，列表较大时成本上升，且不是结构化版本控制。
8. 当前已有 `tests/test_ecn_management_config.py` 与 `tests/test_ecn_management_records.py` 覆盖部分配置、提醒、方案分类、完整性及驳回逻辑；状态路由、并行会签、并发编号和最终执行/重试仍应继续补测试。
9. 上传文件使用原始文件名拼接到上传目录；需进一步核实同名覆盖、文件名净化、类型/大小限制和孤儿文件清理策略。
10. `execution_info` 的编辑可用性主要跟流程状态绑定，数据在审批动作时一并保存；需确认业务上是否应限制为特定执行角色或字段负责人。
11. 方案关联区虽然标注“必填”，保存时只校验方案内容、项目和概述数据有效性；要求序号及关联资料/物料可为空。整体覆盖率能拦截遗漏的强制资料/物料，却不能保证每条方案都关联某条 ECR 要求。
12. 2026-08-18 使用真实 Pyright CLI 对完整 `src/`、`tests/` 扫描时，仍有 27 条既有诊断，集中在 `src/pages/statistics.py`、`src/tools/material_matcher.py` 和 `src/user_service.py` 的 pandas 类型推断与缓冲区协议；它们不属于本次概述配置改动，后续修改这些模块时应优先清理，不能把当前基线误报为全工作区零诊断。

## 5. 像素数据统计分析工具

- 工具实现：`src/tools/pixel_statistics.py`；入口位于分析工具页，权限键为 `pixel_statistics`。
- 处理流程：上传 Excel/CSV -> 数值校验 -> 可选颗粒度分块平均 -> 中心定位 -> 区域筛选 -> 统计 -> 预览及 Excel 导出。CSV 支持 UTF-8、UTF-8 BOM、GBK/GB18030 及常见分隔符自动识别，并按单工作表处理。
- 中心算法包括数据几何中心、全局最大值位置、手工坐标，以及“最大值百分比阈值区域中心”。阈值算法只使用包含全局最大点且面积最大的 8 邻域连通区域，并以区域几何质心为中心，避免远处孤立亮点拉偏。
- 颗粒度合并按实际矩阵居中裁剪，可选让合并后行列数保持奇数；固定尺寸模式会严格拒绝尺寸不匹配的工作表。
- 矩形统计范围严格选择指定数量的行列，不沿用旧脚本包含边界而多算一行一列的口径。毫米范围按处理后比例尺换算后必须完整落在数据表内；矩形或圆形超界时直接报错，不再裁小为表内残缺区域继续统计。
- 比例尺以“原始像素点数对应多少毫米”配置，两个输入均支持最多 6 位小数；圆形半径及矩形长宽按毫米输入。启用颗粒度合并后，系统会按颗粒度自动换算处理后网格的像素密度，全程保留浮点比例尺，不先取整。
- 使用最大值百分比区域中心时，结果区额外绘制主连通区域边缘和几何中心示意图。坐标轴显示整张处理后数据的毫米范围，绘图区按数据物理长宽比设置，保证横纵方向毫米单位 1:1 显示。边缘蓝点是达到阈值的主连通区域的离散边界像素，点值满足但不一定等于判定阈值；悬停提示同时显示 X、Y、边缘点数值和判定阈值。完整区域用于中心计算，示意图每张最多抽取约 800 个边缘点；6 张图以内一次生成，超过 6 张时先生成 6 张并在滚动到底部时每次追加一行（2 张），宽屏始终每行两张。
- 工具界面将比例尺、中心、统计范围、矩阵均匀性和颗粒度作为常用设置；宽屏采用五列等宽、等高卡片，窄屏响应式降为两列或一列。尺寸校验、异常单元格及奇数网格放入默认收起的高级设置。统计结果默认只显示最终指标，处理尺寸、比例尺、定位与裁剪信息放入折叠明细；常规、矩阵和明细表的表头及单元格统一居中。
- 上传区和常用设置在宽屏下采用左、右双栏布局，窄屏自动恢复上下排列；阈值边缘与中心示意图板块默认收起。
- 宽屏双栏由常用设置区决定高度；上传文件标签超出该高度后只在上传卡片内滚动，不再把整行布局撑高。
- 上传器隐藏内部文件列表和 Quasar 原生累计上传大小，使用下方文件标签作为唯一文件状态和删除入口；当前文件数与总大小由服务端现有文件列表实时汇总，删除标签时同步清理上传器队列和服务端分析数据并立即扣减总大小。
- 非圆形统计区域可启用矩阵均匀性统计：把当前全域或矩形区域按用户指定的竖向×横向数量等分，在每格中心按毫米边长取正方形样本并先求均值，再对这些格均值计算常规指标。页面及汇总字段均以“矩阵”前缀区分，Excel 另附每个工作表的采样矩阵。
- 结果不再显示或导出总体标准差、样本标准差，只保留相对总体标准差和相对样本标准差。标准差仅作为计算相对指标的内部中间量。
- 常规统计和矩阵均匀性统计均包含“极值最大偏差/平均值”指标，公式为 `max(|最大值-平均值|, |最小值-平均值|) / 平均值`；平均值为 0 时结果按不可定义处理，页面显示“—”，Excel 写为空值。
- 统计结果表的公式列在表头显示帮助图标；鼠标悬停时展示公式定义及除零、样本数不足等不可定义条件。常规与矩阵公式说明由同一字段映射集中维护。
- 导出文件包含统计汇总、处理参数和矩阵均匀性采样工作表；启用颗粒度合并时还包含合并后的数据矩阵工作表。

## 6. 维护约定

后续处理项目任务时，优先在本文追加以下信息：

- 新读懂的模块职责、入口、关键数据键及外部依赖。
- 已确认的业务规则和角色权限，不记录未经验证的猜测为事实。
- 重要设计决策及原因。
- 已复现的缺陷、修复位置、覆盖测试和仍存在的风险。
- 配置、数据库结构、启动方式或部署方式发生变化时，同步更新对应章节和“最近更新”日期。
- 新增或修改上传交互时，优先使用 `src/custom_ui.py` 的 `custom_upload()`，保持文件移除、清空、大小汇总和样式行为一致；只有在自定义控件明确无法满足需求时才直接使用 NiceGUI 原生 `ui.upload`，并记录原因。

静态类型质量约束：

- 新增或修改 Python 功能在交付前必须同时执行语法编译、相关专项测试和编辑器静态类型检查；不能只以运行成功作为完成标准。
- 静态类型检查必须覆盖完整受影响引用链，并定期覆盖整个 `src/` 与 `tests/`；不能只检查直接改动文件。尤其要检查测试夹具中的异构字典推断、可空返回值收窄，以及框架返回的 `object` 在调用属性前是否完成 `isinstance` 判断。
- VS Code Pylance 扩展中的 `dist/pyright.bundle.js` 是语言服务器构件，不可当作 Pyright CLI。命令无输出且退出码为 0 不代表类型检查通过；必须以编辑器 Problems 的工作区诊断或真正可执行的 Pyright CLI 结果为准，并实际确认诊断数量为零。
- 不用宽泛的联合类型字典向参数类型更严格的数据类或函数做 `**kwargs` 展开；统计结果等固定结构应优先使用数据类或 `TypedDict`，让每个字段的可空性准确表达。
- `T | None` 值在访问属性或传给要求 `T` 的参数前必须显式判空或收窄。由列表筛选、外部框架装饰器等产生的隐含条件，不假定静态分析器能自动推断。
- 异构 JSON 根字典不能只靠第一个字面量推断类型。例如先以 `{"0": {"file_dic": {}}}` 初始化、后续又写入字符串或布尔值时，应显式标注为 `dict[str, object]`（结构固定时优先 `TypedDict`），并在调用嵌套字典方法前用 `isinstance` 收窄。避免再次出现 Pylance 将整个字典误推断为 `dict[str, dict[...]]` 后拒绝写入 `str` / `bool` 的问题。
- NiceGUI 类实例中的可刷新方法必须使用框架专用的 `@ui.refreshable_method`，首次渲染用 `self.method()`，后续用 `self.method.refresh()`。不能用 `@ui.refreshable` 配合 `ClassName.method(self)` 绕过类型提示：这种写法会把刷新目标登记到空实例，导致后续文件列表、结果区等实例 UI 无法刷新。
- 不使用 `type: ignore`、关闭诊断或把类型扩大成 `Any` 来掩盖本可建模的问题；确需例外时必须记录框架限制和运行验证依据。

不要在本文记录密码、密钥、个人信息、真实业务单内容或其他敏感数据。
